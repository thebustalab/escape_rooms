Read all four evidence files plus the room's actual state on disk. Summary of what's established for this room:

- The spec **already carries a bounded mover with authored vigour**: the caged inspection lamp on a long flex, `at: "just left of centre"` (→ x 0.36 in `_POS`), with its vigour written into the `desc` ("swinging slowly and throwing the box-fronts in and out of relief"). Every `at` in the spec resolves in the closed vocabulary — "dead ahead in the centre" → 0.50, "just right of centre" → 0.64, "on the far right" → 0.92.
- The still is **committed and not stale**: `rooms/flat_clustering/heist/a_vault/scene.png` (2026-09-05 21:57) is newer than `scene_specs_heist.json` (2026-09-03 21:55), and `scene.judge.json` records a human-triaged `hold` with a clean plain-steel seam.
- The motion prompt at `cinemagraph_tools/art_prompt_motion/heist/a_vault.txt` is already the house form, ~35 words, naming the lamp and nothing else.
- The ledger is unambiguous about this room: `a_vault` is **the strongest room measured anywhere** — return 0.91, alive in all 16 cells of its factorial grid, no camera motion in any cell.

No clip exists, gate report is empty, nothing has been tried. There is nothing to author and nothing to soften — the only thing missing is the render itself.

DECISION: RENDER
WHY: The spec already depicts a bounded mover with authored vigour (the caged lamp swinging on its flex), the committed still is newer than the spec and seam-clean, the motion prompt names that mover and nothing else in the house form, and no clip exists yet — so the one render at the pinned default is the next step.
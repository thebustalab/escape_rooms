I read the four evidence documents, rendered both prompts from the on-disk spec, and looked at the committed still at native resolution.

**State:** `scene.png` was regenerated today and is post-occlude (`authoring.seam.stage: "occluded"`, 16:26). The still depicts exactly one bounded mover — a single compact cigar-smoke ribbon rising from the ashtray on the near baize at x≈0.19, two or three legible coils, confined to a narrow column, the rest of the room's air clear with no haze layer. That is the authored-mover condition the whole loop rests on, satisfied. The motion prompt resolves from the spec (35 words, house form) and names that plume and nothing else. The `rigid` clause is two items — the pasted-in marble pilaster and the damask behind it — which is scalpel-length and correctly pins the composited seam occluder rather than an inventory. No clip exists; gate report is empty; nothing has been tried.

DECISION: RENDER
WHY: The still depicts one bounded, legible, non-frame-filling mover with its vigour authored into the `desc`, the spec-derived motion prompt names only that mover, the rigid clause is two items pinning the composited seam pilaster, and no clip has been rendered yet — so the only thing left is the one render at the pinned default.

One thing to flag for your eye, not acted on this cycle: the spec carries **two elements with `id: "smoke"`** — the compact plume (`moves: true`) and a stale leftover "slow bank of cigarette smoke hanging over the near baize… barely moving" (`moves: false`). Both feed the art prompt, so it currently asks for a haze bank and, one clause earlier, for no haze anywhere. The render resolved that contradiction the right way — there is no bank in the image — so removing it would cost a fresh panorama plus the whole screen/blur/occlude chain on art that is demonstrably correct. Cheap to delete next time the still is regenerated for another reason.
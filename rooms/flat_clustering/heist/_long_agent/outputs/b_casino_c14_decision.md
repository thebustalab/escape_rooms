I've read all four documents. Here's what they settle for this room:

**b_casino is already an authored, evidenced room.** It is one of the four `heist` rooms that animated in **all 16 cells** of the calibration factorial (`compare_cal_b_casino.mp4`, ledger line 1070: *"distinct per seed; only the smoke moves"*), and it is one of the five rooms confirmed alive **at full resolution** by Lucas's own eye at both 0.70 and 0.65 (ledger, "Full-resolution results at 0.70 / 0.65 … all five rooms alive by eye").

The spec matches the pipeline's requirements without edits:
- **Bounded mover present** — `smoke`, a bank under *one* low table lamp over the *near* baize. Bounded and diffuse, which is the confirmed-good shape, not `hood`'s frame-filling cloud sea.
- **Vigour authored** — `motion.vigour` is set, so `element_desc` appends it to the art prompt (`scene_spec.py:391`). The depicted vigour is already quiet ("coiling and rolling slowly"), consistent with Lucas's label "alive (subtle smoke)". Subtle is enough; `market_price` returns 0.23 and ships.
- **Motion prompt is derived, not hand-written** — the given prompt is byte-identical to what `render_motion_prompt` emits from `motion.phrase`, in the house ~30-word form naming only the smoke.

Gate report is empty and nothing has been tried, so there is nothing to soften, pin or re-author. One expected note for whoever reads the gates: **b_casino's return fraction is 0.00 and that is not a rejection.** It is the canonical cyclic-flow case — the retraction on ledger line 1303 is about this exact room. `cine_return_check` escalates a zero; it must not be read as dead.

DECISION: RENDER
WHY: The spec already carries a bounded, vigour-authored mover that is evidenced alive at 3072 in the working band, the motion prompt derives from that same mover, and no clip exists yet — so the one render at end guide 0.70 is the next step.
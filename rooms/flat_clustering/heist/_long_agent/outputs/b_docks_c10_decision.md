Evidence read. State of `b_docks`:

- **Spec** carries one bounded authored mover (the tarp, with `motion.vigour` + `motion.phrase`), and the calibration grid `compare_cal_b_docks.mp4` shows it alive in all 16 cells — this is one of the four `heist` rooms cited in `notes/z_archive/report.md` (archived) §2 as the *proof* that animatability is authored.
- **Motion prompt** on disk is byte-for-byte the house template wrapped around the tarp's own `phrase` — one spec, two consumers, no drift.
- **Clip exists**: `temp/cine/_art_prompt_exp/heist/b_docks_motion_ri8_looped.mp4`, seed 4242, rendered 17:36–17:37 today. Cycle 8's RENDER was carried out.
- **Gates, all three, all pass**: camera *still* (frame0_corr 0.94, landmark drift 0 px, loop jump 1.79); glitch **breadth 0.0** against the enforced 0.667 — nowhere near, and `spike` 2.89 is reported-only and was never validated as a threshold, so it is not a rejection; return **0.31**, non-zero and sitting mid-range of the shipped Egypt clips (0.13–0.51), so not even the ambiguous-zero escalation.

Nothing in the report is a reason to re-author, re-vigour, soften, or pin. Nothing in it is grounds to accept either — no gate may approve, and the eye decides.

DECISION: HOLD
WHY: The authored tarp rendered once at the pinned default and the clip cleared all three reject-only gates — camera still, glitch breadth 0.0 against the 0.667 line, return 0.31 rather than an ambiguous zero — so the only step left is Lucas's eye, which this queues.
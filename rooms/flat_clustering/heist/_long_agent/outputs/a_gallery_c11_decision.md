Evidence read. Findings for `a_gallery`:

- The spec already carries exactly **one bounded mover**: the `sheet` element, a canvas dust sheet over a near plinth, with `motion.moves: true` and an authored vigour string ("lifting and settling in the draught from a propped fire door"). Bounded, cloth — the strongest confirmed mover category in the ledger.
- `scene_spec.render_motion_prompt` on this spec reproduces the live motion prompt byte-for-byte, so art and motion derive from one authored intent and cannot have drifted.
- The still is committed and human-triaged — rooms/flat_clustering/heist/a_gallery/scene.judge.json, verdict `hold`, clean plain-marble seam, no repair needed.
- report.md §2 records this exact room and this exact mover as alive in **all 16 cells** of compare_cal_a_gallery.mp4 — a_gallery is one of the four heist rooms cited as the positive control for authored animatability.
- No clip exists (no mp4 anywhere for a_gallery), gate report is empty, nothing tried yet.

So there is nothing to author, nothing to soften, and nothing to pin. The art is right, the prompt is right, and the only missing thing is the render itself. Re-authoring here would discard a seam-accepted still to chase a room the evidence already calls alive.

DECISION: RENDER
WHY: The spec carries one bounded, vigour-authored cloth mover whose motion prompt derives from that same spec, the still is committed and seam-clean, and a_gallery animated in all 16 cells of its calibration grid — so the only step left is the single 3072x1024 render at end guide 0.70.
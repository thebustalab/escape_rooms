Read the pipeline, the report's metric section, and the ledger entries that name this room. The decisive evidence is already on file:

- **b_casino is the canonical cyclic-flow room.** The ledger's 2026-09-11 entry records Lucas watching this exact room's smoke and saying it's *"alive at both settings… a cyclic thing with one full cycle per loop"* while it measured return **0.00**. The return gate detects out-and-back motion, not motion, and it may escalate but never reject. Reading 0.01 as dead here would repeat the specific mistake the ledger logs three times.
- **The glitch gate passes cleanly**: breadth 0.042 raw and 0.042 normalised, against a threshold of 0.70 on *both* readings. `spike` is reported, never enforced.
- **Camera is still.**
- The lower peak (4.19, against the old spec's 8.06) is consistent with the softening this re-authoring was for — Lucas's stage-4 note on this room was that the smoke moved *"way too quickly"*. Peak amplitude is explicitly not a threshold for cyclic movers, so it isn't grounds to act.

All three wired gates are reject-only and none rejected. Nothing in step 6 applies: no eye has called it dead, no camera drift, nothing spurious moving.

DECISION: HOLD
WHY: The clip cleared all three gates — camera still, glitch breadth 0.042 on both readings against a 0.70 threshold — and its near-zero return is the documented cyclic-flow signature of this room's smoke rather than evidence of death, so the only step left is Lucas's eye.

**Spoken summary:** The casino clip passed every automatic check. Its "return" number is almost zero, but that's expected here — smoke flows one way round a loop instead of swinging out and back, and this is the very room where you told us that reading is misleading. The smoke is also moving less than the old version, which is what we wanted after you said it was going way too quickly. Nothing to fix from the machine's side; it's queued for your eye.

One thing worth your attention when you look: the spec still carries **two** smoke elements — the new compact plume, and an older "slow bank of cigarette smoke hanging over the near baize" that the new one explicitly contradicts. Both feed the art prompt. I left it alone because fixing it means regenerating the whole panorama, but if the still shows a general haze as well as the plume, that duplicate is why.
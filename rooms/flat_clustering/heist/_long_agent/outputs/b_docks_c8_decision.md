Read the four evidence files and checked the live tree. Findings for `b_docks`:

- **Bounded mover present and authored.** `tarp` — a heavy canvas tarpaulin at the open loading hatch — carries `motion.{moves, vigour, phrase}` on disk, so `_has_authored_mover` is satisfied. Hanging cloth/canvas is the ledger's strongest confirmed mover category, and it's bounded, not frame-filling.
- **The art demonstrably animates.** The calibration factorial has b_docks alive in **all 16 cells** with crisply nameable per-seed types (billowing / swishing / opening-and-closing) and **no camera flags reported** — one of the four heist rooms that are the evidentiary basis for the whole "animatability is authored" finding.
- **Motion prompt is correct and consistent.** `art_prompt_motion/heist/b_docks.txt` is byte-identical to what `render_motion_prompt` derives from `motion.phrase`, names only the tarp, in the house ~30-word form.
- **Still is committed and seam-clean** (`needsWork: false`, screened 2026-09-05).
- **No pipeline clip exists.** Gate report is empty and the tried-list is empty; every b_docks artifact in `temp/cine/_art_prompt_exp/heist` is from an earlier experiment (the `cal*` 1536 probe grid and the `y50`/`y55` Y-model seed fan), none a render at the pinned default.

Nothing to author, nothing to soften, nothing mis-moving. Deliberately not touching the spec: `last_eval` on the sibling room records that any spec edit re-renders the art prompt and would silently overwrite the committed 1782-char `scenePrompt` with the current renderer's 3121-char output, unrecoverably in an unversioned tree. RENDER touches none of that.

DECISION: RENDER
WHY: The spec carries a bounded, authored-vigour mover that the calibration grid shows alive in all 16 cells, the motion prompt derives byte-identically from that same spec and names only the tarp, the still is committed and seam-clean, and no clip exists yet — so the single render at the pinned 0.70 default is the only remaining step.
Evidence read. State of `b_docks`:

- **Spec** carries exactly one bounded authored mover — `tarp`, with `motion.{moves, vigour, phrase}` — and it is the room `report.md` §2 cites as alive in all 16 cells of its calibration grid.
- **The art is new and it is the re-author Lucas asked for.** His eye-note was an ART note ("the docks tarpaulin a bit big"). The `desc` was rewritten at 22:01 on 2026-09-13 from "lashed at the open loading hatch" to "lashed back to one side… a narrow panel of it hanging loose clear of the opening", the vigour narrowed to "its loose lower corner", and `scene.png` regenerated at 22:23. I looked at the left band of the panorama at native scale: the tarpaulin is now a single lashed-back vertical panel against the hatch jamb, roughly 6% of frame width, with its lower corner hanging free — bounded, hanging canvas, the ledger's strongest mover category, and plainly smaller than what he commented on.
- **The prompt is clean.** `scenePrompt` on disk is byte-identical to `scene_spec.render_prompt` of the current spec (3242 chars, equirect clause present) — the stale-cached-prompt trap is not open here. Seam screened 22:24, `needsWork: false`.
- **No clip exists for this art.** The newest render, `b_docks_motion_ri8`, is 2026-09-13 17:37 — about five hours older than the still and produced from the pre-feedback prompt. Everything else in `temp/cine/_art_prompt_exp/heist` is the 1536 `cal*` grid or the `y50`/`y55` Y-model fan.

Nothing to author, soften, re-vigour or pin — and with no gate report there is nothing to diagnose. One note for the record, not a change: the motion prompt quoted in the brief ("billows and slackens") is the stale `art_prompt_motion/heist/b_docks.txt` fallback; because the spec declares a mover, `render_motion_prompt` resolves from `motion.phrase` and the render will use *"locked-off static camera, zero camera movement. The loose lower corner of the lashed-back tarpaulin flapping and snapping in the wind off the water. Only that moves; everything else stays perfectly still. Seamless natural loop."* — the softened phrase that matches the new art.

DECISION: RENDER
WHY: The re-authored still post-dates the only existing clip by five hours and depicts a materially different, correctly bounded tarpaulin, so this art has never been rendered — it needs the one sanctioned 3072x1024 pass at the pinned 0.70 default before any further judgement is possible.
Evidence read. State of `a_opera` after cycle 20's single render:

- **All three gates cleared.** Camera `still` (frame0_corr 0.963, landmark drift 8 px, loop jump 1.45 — no drift). Glitch breadth 0.0 raw and 0.0 normalised, against a threshold of 0.70 that requires *both* readings to exceed it; `spike` is reported, never enforced. Return 0.13 is **non-zero**, so `cine_return_check`'s ambiguous-zero escalation does not fire.
- **0.13 is inside the shipping range, not below it.** The committed Egypt clips return 0.13–0.51 and the ledger's calibration ranks them the way Lucas does; `market_price` returns 0.23 and ships. A non-zero return means the trace traced an arc and came home — an out-and-back mover, which is exactly what a swag curtain breathing in a draught should read as. Peak 3.6 sits with `b_casino`'s alive 3.8, not with hood's dead 5.1 (whose deadness showed as return 0.00, not as a low peak; the peak-amplitude tell only applies when the return is zero).
- **No fix step applies.** It is not too lively (no basis to soften, and softening works down only). Nothing that should be still is flagged as moving. It is not dead by any measure we have — and step 6 sends a room back to the art only when it is dead, which this is not.
- The runner's own verdict already reads *"survived gates — needs Lucas's eye"*, at `/home/bustalab/Documents/Tools/temp/cine/_art_prompt_exp/heist/a_opera_motion_ri20_looped.mp4`.

No metric may approve a clip, so this is not acceptance — it is the handoff to the one step that can decide.

DECISION: HOLD
WHY: The single allowed render cleared the camera, glitch and return gates with a non-zero 0.13 out-and-back return inside the committed shipping range, so there is nothing left for a metric to reject and the clip belongs in front of Lucas's eye.
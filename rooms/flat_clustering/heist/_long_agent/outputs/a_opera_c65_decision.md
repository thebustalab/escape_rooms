I read the four authority files, then checked the room on disk rather than trusting the cycle-64 summary.

**Cycle 64's pin was never delivered.** It emitted `SPEC: {"rigid": "..."}` — a JSON *object*. The runner's `PIN_NON_MOVERS` branch (`long_agent.py:3886-3899`) sets `spec["rigid"]` only for a bare **string or list**; a dict falls to the element branch, finds no element with a matching `at`, and inserts it into `elements` at `len(els)//2`. That is exactly where it now sits, position 3 of 7. `render_motion_prompt` reads only `spec["rigid"]` or a per-element `motion.still_as` — neither of which an element-level `rigid` key is — so the fragment is inert in both prompts. I confirmed by rendering from the live spec: the motion prompt still carries the OLD clause naming "both curtained doorways", the precise wording cycle 64 proved insufficient. The progress line *"PIN_NON_MOVERS applied. Both prompts re-rendered"* is true of the call and false of the effect.

**So the defect stands unfixed and the fix is unchanged — only its shape was wrong.** Re-issuing as a JSON string literal lands in `spec["rigid"]` and the escalation fires (`rigid=bool(spec.get("rigid"))` at `exp_art_prompt.py:100`). I simulated the exact fragment through `json.loads` → the apply branch → `render_motion_prompt(rigid=True)`: the drape clause appears, and every item the old clause pinned — parapet, walls and mouldings, tiers, writing-slope, terminal, lighter — is retained, so the breathing-wall fix Lucas asked for is not lost.

I tightened cycle 64's wording. Its version narrated the drapes "tied back at their tassels, both openings left wide, lit and unobstructed" — a *state* the committed still may not depict, which is asking the clip model for art that does not exist, the drift the one-spec design exists to prevent. Mine pins the drapes against the motions actually observed (drawing, sweeping) and adds no depiction.

**`render_prompt(spec)` is byte-identical before and after this edit** — verified — so `scene.png` remains valid art for the live prompt. The runner will nonetheless set `needs_still` unconditionally on any spec edit; that block is spurious for a rigid-only change, and it is already set from cycle 64, so this decision does not worsen the room's state.

DECISION: PIN_NON_MOVERS
SPEC: "the heavy velvet portière drapes hanging at the two doorways on the right, which never draw, sweep or swing across either opening, along with both doorways themselves, the gilded box parapet, the panelled walls and their mouldings, the auditorium tiers and boxes beyond, the writing-slope and the terminal, and the lighter"
WHY: Cycle 64's pin was emitted as a JSON object and the runner inserted it as an inert element instead of setting `spec["rigid"]`, so the motion prompt still carries the old clause that names the doorways but not their drapes — re-issuing the same fix as a bare JSON string is the only shape the apply branch routes to the rigid list.
I opened the strips myself rather than trusting the write-up: `s1`–`s6` at 512px native plus a wrapped right-300/left-300 seam crop, all under `temp/art_qc/heist/judge_st/`. The worker's findings hold on every checkable point — terminal on the desk in s3 (with the velvet-tray cabinet behind it), engraved invitation directly under a lit brass keypad in s4, companionway stair in s4–s5, columned passage away from the viewer in s5–s6, seam clean plain walnut with nothing crossing the join, and the faux-engraved squiggles on the invitation card are real and visible at native res. The steward's table plan is genuinely absent — and I confirmed it structurally too: `scene_specs_heist.json` `b_stateroom` has no `clue` element at all, while `plannedHotspots` demands a pickup clue "The steward's table plan", and every sibling room (`a_vault`/`tag`, `b_docks`/`board`, `a_gallery`/`matches`, `b_casino`, `b_pawnshop`) does have one — so the wiring phase would have nothing to box. I diffed the proposed spec against the current one programmatically: every non-element key is byte-identical, no element removed, no existing element altered, exactly one addition (`plan`) — a clean single-variable change, and attempt 1 was the only prior attempt so this repeats nothing. Correctly restrained not to also chase the invitation lettering in the same spec, since the `negatives` string already forbids it and a second edit would destroy attribution.

VERDICT: PASS
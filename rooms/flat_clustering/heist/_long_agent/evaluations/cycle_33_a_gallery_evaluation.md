I opened `strip1_of6.png`, `strip4_of6.png` and my own left/right wrap composite at native 512×1024, plus the spec itself, and every load-bearing claim checks out: `scene_specs_heist.json` → `a_gallery.elements` has exactly seven entries and no `fob`, strip 1 shows the steel locker with dark keypad flush in dark veined marble and no ledge or evidence bag anywhere beneath it, strip 4 shows the trolley terminal, the stair-hall arch and the matchbook on the floor at full-pano x≈1986 (your x≈1990), and the wrap composite shows baseboard and floor banding running straight through the join with no tone step and no object crossing. So the defect is real, named, upstream, and fatal for wiring — TOKEN 3/5's `clue` hotspot would have nothing to box. I diffed your proposed spec against the current one field by field: zero changed fields, zero changed or dropped elements, one addition (`fob`), so the experiment is cleanly attributable to a single variable and no checklist object is lost. Good call keeping the fob's label blank inside a sealed bag — that satisfies the "no numbers/lettering" negative while leaving the serial to be delivered in hotspot content rather than paint. Burning a strong render hurts, but a spec-level omission genuinely cannot be repaired downstream, and this is attempt 2 with no prior fob attempt to repeat.

VERDICT: PASS
I checked the room's state on disk rather than the gate report.

**The worker's central claim holds.** The committed still for b_casino was written at 15:04 local; the ri98 clip was baked at 14:53–14:54, about ten minutes earlier. The gate report on the table therefore measures art that no longer exists, and nothing has been rendered since.

**I looked at both.** The ri98 filmstrip shows a visibly different room — cashier's cage under a central arch, a rug, red-topped tables — with a low grey haze lying over the near table and only a thin wisp off the cigar. The current still shows one bold ribbon of smoke climbing off the ashtray in three legible coils, confined to a narrow column, the rest of the air clean. That is the bounded mover the spec asks for, not a frame-filling one.

**The prompts check out too.** The prompt file used for the 15:03 draw is byte-identical to the stored scenePrompt — 4302 characters, exact match — so the still really was drawn from this spec. The scenePrompt carries the bounded-plume language plus the stale bank element as desc-only text, which is exactly what cycle 103 set out to do: strip the second mover from the motion prompt while leaving the art prompt untouched.

**Against the rejection list:** no settings change (0.70 is the pin ri98 already ran at, not a new lever), a bounded mover rather than a frame-filling one, no treatment of zero return as death — the worker explicitly says to expect zero and discount it — no clip accepted, nothing written into scenario.json, no hand-written art prompt, and it isn't an AUTHOR_MOVER at all. The one thing genuinely owed here is a render against a still that has never been rendered.

VERDICT: PASS
I verified independently rather than trusting the write-up. The six native strips exist in `temp/art_qc/heist/a_townhouse/` with timestamps matching the run, and I cut my own crops from `l1_a_townhouse_1.png` (3072×1024): the brightened right third and a 2.4× tight crop of the tall gilt mirror at x≈2140–2420 both show the frame completely bare on every edge, and the mid band (x 1536–2200) shows nothing tucked into the dressing-table mirror either — the house manager's card is genuinely absent, so the MID-POINT TRADE hotspot would have nothing to box. I diffed the proposed SPEC against the on-disk `scene_specs_heist.json` entry: it is byte-identical except the `card` element's `desc`, so the change is attributable to one variable, no checklist object is dropped, `seamOccluder` was already there, and this is not a repeat of a prior attempt. The other checklist items check out in my own crops — both doors, the floor terminal, and the chip — and the worker was honest about the two things it chose not to act on (terminal on the floor rather than the table, chip markings on face instead of rim), which is the right call under single-variable discipline. Regenerating is the cheapest sufficient action for a missing clue object.

VERDICT: PASS
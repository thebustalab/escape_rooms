I checked every claim against the files and the image rather than the worker's description.

**Verified independently:**

- `render_prompt(current sceneSpec)` is byte-identical to the committed `scenePrompt` — 11,442 chars both. The art is not stale; it *is* what this spec produced, drogue-kite clause included.
- I opened `ap_stillstand/scene.png` at native 3072×1024 and cropped the `dead ahead in the centre` band (`_POS` → 0.50) and the whole panorama. Above the dash there is cloud, the eyewall and the cityscape, and the dash itself carries the sampler globe, the reading-pane and the six-cell override — **no kite, no line from the nose tip, no tail**. The kite clause sits as the final ~22 words of a 160-word element whose subject is the dash, and the renderer dropped it. Same shape as `ap_pileup`'s buried `torn_cloud` at c7.
- The current motion prompt would therefore instruct *"The towed membrane kite riding and dipping on its line ahead of the nose"* over a crop containing a dash and a city — the exact verdigris/alum_madder failure this loop exists to eliminate.
- The last reject criterion does not bite. It exists to stop re-authoring **before evidence**; the mover *has* been put through the art, where it failed, and the byte-identical prompt proves it was given a fair run. This is the `ap_pileup` c7 situation exactly, which passed for the same stated reason. `max_attempts_per_room: 2` is untouched — this is ap_stillstand's first cycle (c13).
- **The departure from the c7 precedent is correct and I checked its basis.** `notes.md` line 116 authors this room's hero mover as *"a towed drogue kite on a line, riding and dipping"* — Lucas's own per-room table. And the phrase is travel-free under `cinemagraph_pipeline.md` §THE NO-TRAVEL RULE: tethered, oscillatory, *"rippling"* is on the **Name only** list. `ap_pileup`'s cloud had the second fault (*"streaming past… parting around them"*) that forced the subject switch; this one does not. Keeping the kite is the narrower fix.
- I simulated the runner's merge. The fragment is valid JSON, hits `at` exactly at index 3 of 7, merges in place (dash content preserved verbatim, so no `plannedHotspot` object is lost), leaves **exactly one mover**, replaces the stale `animate.loop: "boomerang"` with `crossfade`, and renders a 34-word travel-free motion prompt. The new art prompt is 11,887 chars — it changes, so the runner regenerates the still before any clip, which is the correct order. All four `kite` occurrences in the new prompt fall inside the new element; no stale drogue text survives anywhere, and `soundBed`'s *"the kite line singing"* still agrees.
- Bounded, not frame-filling: *"about the size of a door"*, *"small in the frame"*, with the dash below it. The *"ONE single kite and no other, drawn exactly once"* guard is the right lever against the doubling that catches regenerated stills.

None of the six reject conditions holds. No settings are touched, no clip is accepted, nothing is written to committed art or hotspots, and no prompt is hand-written.

One thing for the runner's awareness, not disqualifying: promoting a door-sized kite to the head of the centre slot puts it directly above the READING-PANE, and `eye_knot` sits one slot left — if the regenerated still crowds either, that is an `ADJUST_VIGOUR` on size at the next cycle, not a reason to block this one. The worker's closing note about `notes.md` line 115 still listing `ap_pileup`'s retired torn cloud is accurate; it is a docs matter for a human, outside this loop.

VERDICT: PASS
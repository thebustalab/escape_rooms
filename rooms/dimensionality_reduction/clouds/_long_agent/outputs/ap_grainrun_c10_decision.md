Everything checks out. Verified independently:

- **Clip exists and is complete** — `ap_grainrun_motion_ri8_looped.mp4`, 3072×1024, 24 fps, 61 frames (73 raw minus the mandatory 12-frame / 0.5 s crossfade), baked 07:40 from the 07:24 still, which is itself fresh from the 07:24 spec edit.
- **Render params are the settled recipe exactly** — end guide 0.70, cfg 2.0, 8 steps, 73 frames, 3072×1024, and **seed 4711**, the seed Lucas approved this motion recipe at.
- **Motion prompt is byte-identical to the approved cockpit prompt**, 38 words, rendered from the spec (not hand-written): the city brightening/dimming plus lightning rippling through the storm — the two sanctioned movers, no retired hero mover re-authored.
- **The still depicts both movers** — the lit city receding low and left, the structured storm wall right, dense charcoal vapour in front, grey palette, dash sharp in the foreground with globe, reading-pane and six cells.
- **Every gate cleared.** Camera still. Return 0.65, non-zero, so no dead-or-cyclic escalation. Dead false at peak 12.11 / ratio 13.97 — so the "inside a cloud means quieter" caveat never bites. Glitch raw breadth 0.521 is below 0.70, and the gate requires **both** readings above 0.70 to reject, so the 0.875 normalised reading alone convicts nothing — that is precisely the a_vault light-mover false positive the two-reading rule exists to prevent, and this room's movers are lights.

No lever is indicated: nothing is too lively, nothing wrong is moving, nothing is dead, and a clip already exists from the current spec.

DECISION: HOLD
WHY: The clip is a fresh, complete bake of the current spec at the approved recipe and seed 4711 with the exact 38-word cockpit-approved motion prompt, and it cleared the camera, return, dead and glitch gates — the single high normalised-breadth reading is the known light-mover false positive that the both-readings rule correctly declines to convict on — so it goes to Lucas's eye unaccepted.
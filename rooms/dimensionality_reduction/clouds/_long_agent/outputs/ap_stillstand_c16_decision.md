I read the four authority files, then checked this room's actual state against them.

**What I found**

- **The c13 AUTHOR_MOVER landed and the art was regenerated.** The committed spec's `kite` element now sits at the head of the `dead ahead in the centre` slot, carrying `motion.{moves, vigour, phrase}`, and it is the **only** mover in the spec (the other six elements carry none). `scene.png` and `scenario.json` were both written at 2026-09-22 23:32, after the spec edit, and `generations: 2`.
- **The art is not stale.** `scene_spec.render_prompt(current sceneSpec)` is byte-identical to the committed `scenePrompt`, 11,887 chars both — the new length the c13 evaluation predicted, not the old 11,442. This still *is* what this spec produced.
- **And this time the still DEPICTS the mover.** I opened `ap_stillstand/scene.png` at native 3072×1024 and cropped the centre band (`_POS` → 0.50). There is **one** pale membrane kite, veined and translucent, flown on **one** long taut line running down to the nose tip, with a ribbon tail streaming below it — roughly 130 px across with a mover complex of ~300×400 px in a 3072×1024 frame. Bounded and small, not frame-filling, near-field and high-contrast against the dark eyewall. The `"ONE single kite and no other"` singularity guard held: nothing elsewhere in the panorama duplicates it (the angular stained-glass shapes on the left are plainly PIGMENT buildings). This is the exact failure c13 diagnosed, and it is fixed.
- **The regeneration cost nothing that was working.** The evaluator's one flagged risk — a door-sized kite promoted to the centre slot crowding the reading-pane or `eye_knot` — did not materialise. The dash still carries all three instruments (sampler globe left, milky reading-pane centre, six dark cells in a triangle beside the lever right), and `eye_knot` one slot left is intact as a lumpy, closed clot of bruised cloud over the city's heart, no spiral and no eye. No `plannedHotspots` object lost its depiction.
- **The motion prompt is well-formed and travel-free.** Spec-derived (34 words, house form, one mover named and nothing else). "Riding and dipping on its line" and "tail rippling" are tethered and oscillatory — on the no-travel rule's *Name only* list, with no net path to return from.
- `attempts` is 0 against `max_attempts_per_room: 2`, `last_gate` is `{}`, and no clip exists at any tag for this room.

**What I am not doing.** No `ADJUST_VIGOUR` to enlarge the kite. It is small, and a small mover is the nearest thing to a risk here — but the pipeline's answer to that is a clip, not a pre-emptive regeneration: subtle motion is enough, `vigour` feeds the ART prompt so editing it discards a panorama that has just correctly delivered the authored mover, and "dead → go back to the art" is a step-6 remedy *after* evidence. No pre-emptive `rigid` clause either, for the same reason and because a generous one causes a worse fault than it fixes. Nothing left to author; the only step remaining is the single render.

DECISION: RENDER
WHY: The c13 re-authoring worked — the regenerated still now visibly depicts exactly one bounded, tethered drogue kite with its line and rippling tail, the art prompt is byte-identical to the current spec so the still cannot predate its own motion prompt, the spec-derived motion prompt is 34 words and travel-free, and no clip exists yet, so the next step is the one render at end guide 0.70.
I read the four authority files, then verified the defect from the frames myself rather than from the prior cycle's account.

**What the record shows.** Cycle 25 rendered once at the settled recipe; cycle 27's HOLD was **failed by an evaluator that pulled frames**; cycle 28 diagnosed it and chose PIN_NON_MOVERS, but its evaluator returned **REVIEW, not FAIL** — the judgement was sound and the *payload type* was wrong. The spec on disk still has `rigid: None`, so nothing was applied and the gate report I was handed is still ri25's.

**The defect, confirmed independently.** I extracted frames 0, 18 and 36 from the raw render. Frame 0 is the clean committed panorama. By frame 18 a gridded ochre sailcloth spans the full 3072 px; by frame 36 it has buried the city's right flank, the eyewall, the ring-current and the derelict itself. Its roots are visible: it grows out of the viewer's **own two foreground membrane wings**, which sit in the bottom-left and bottom-right corners of the still in exactly that tan translucent material, and unfurls them into one sail. The gates cannot see it by construction — `return 0.69` is high *because* the intruder arrives and leaves, the glitch breadth 0.125 caught only the onset edge of a 55-frame growth, and the camera genuinely is still.

**Why the rigid clause, and why only one item.** Step 6.2 of the fix ladder is exactly this case: something that should be still is moving. I ran the code rather than estimating. With `rigid` as the **string** the renderer requires, `render_motion_prompt(rigid=True)` comes back at **57 words** — inside the 61-word register proven safe, far from the 176-word case that returned CAMERA MOVES — and `render_prompt` is **byte-identical**, so the committed panorama is untouched and no reviewed art is discarded. Cycle 28 also listed the nose and dash; across all three frames the dash and nose are perfectly static, so naming them is inventory rather than scalpel, which is how `a_opera`'s clause grew to seven items and broke the camera. One item, the implicated one.

DECISION: PIN_NON_MOVERS
SPEC: {"rigid": "the viewer's own glider's two wide translucent membrane wings spread across the foreground"}
WHY: Frames 18 and 36 show the video model unfurling the viewer's OWN frame-filling foreground membrane wings into one full-width ochre sail instead of flapping the derelict's small torn wing they share a description with, so the remedy is the step-6.2 rigid clause naming those wings alone — typed as the string `scene_spec` requires, rendering a 57-word motion prompt and a byte-identical art prompt.
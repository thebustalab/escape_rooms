Evidence read, and the room's state on disk verified against it.

**What's there now:** the cycle-4 render exists (`temp/cine/_art_prompt_exp/clouds/ap_grainrun_motion_ri4.mp4` plus its baked loop), at the settled recipe exactly — 3072x1024, end guide 0.70, cfg 2.0, 8 steps, 73 frames, seed 4242. The spec carries one bounded mover, the pale membrane ribbon tied to the strut close to the viewer, vigour "brisk and snapping". The committed art prompt is byte-identical to the current spec's `render_prompt`, so the still cannot be stale, and the spec-derived motion prompt is the 30-word house form naming an oscillatory phenomenon with no net travel.

**The gates, one by one:** camera reads "one object moving (camera still)" — not a drift rejection. Glitch breadth is 0.0 on both the raw and the brightness-normalised reading, against a threshold of 0.70 that needs both. The dead gate reads ratio 15.3 against a floor of 1.75 and peak 7.2 against 0.45 — not merely alive, but strongly localised, which is what a bounded mover should look like. Return is 0.24, above the 0.05 alive line and sitting right among the shipped egypt clips (quay 0.23, library 0.25).

Nothing rejects, and nothing in the report points at over-liveliness — softening the phrase on no evidence would be a guess, and intensity language only works downward anyway. So this survives to the eye.

DECISION: HOLD
WHY: The clip renders at the settled recipe from a spec whose single bounded mover the committed art depicts, and it clears every reject-only gate — camera still, glitch breadth 0.0 on both readings, dead ratio 15.3 with peak 7.2, return 0.24 in the band of already-shipped clips — so there is no defect to act on and the only remaining step is Lucas's eye.
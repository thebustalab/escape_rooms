Everything checks out against the evidence. The decision:

**Verification performed (no side effects):**
- Spec carries exactly one mover — `eyewall`, `motion.vigour` "slow and immense, a steady turning", `motion.phrase` the twisting funnel. Every other element `motion: null`.
- `render_prompt(sceneSpec)` is byte-identical to the committed `authoring.scenePrompt` (10473 chars) — the art cannot be stale.
- `render_motion_prompt` yields the 30-word house form, one oscillatory mover (a column turning on itself), no net travel.
- Render params are the settled recipe exactly: 3072×1024, end guide 0.70, cfg 2.0, 8 steps, 73 frames, seed 4242. One attempt, no rolls.
- Gates, all reject-only, all clear: camera "one object moving (camera still)"; glitch breadth 0.479 raw / 0.500 normalised — **both** below the 0.70 threshold, so no rejection and nothing for me to re-tune; dead `false` at ratio 12.17 (floor 1.75) and peak 7.49 (floor 0.45); return 0.36, non-zero and above every held sibling in this scenario.
- The difference map on the contact strip shows motion concentrated on the funnel at the right, with the ring-current band beneath it and the two lit dash objects (sampler globe, reading-pane) shimmering. The city, the wings, the shell dash and the nose are near-black in the diff — still. No camera drift, no frame-filling wash. The globe's drift is authored intent (it is `cockpit`'s own accepted mover), so there is nothing to pin.

DECISION: HOLD
WHY: The single render at the settled recipe came from a spec whose one bounded mover the committed art demonstrably depicts, and it cleared every reject-only gate with margin — camera still, glitch below threshold on both readings, not dead, return 0.36 — with motion localised to the named funnel, so the only remaining step is Lucas's eye, which no metric may pre-empt.
I've read the four source files and the runner code. The decision:

DECISION: AUTHOR_MOVER
SPEC: {"id": "still_flood", "at": "to the left", "desc": "black floodwater lying flat across the sunken left half of the floor, dark and deep, its edge stopping two steps below the bronze flood door", "motion": {"moves": true, "vigour": "its surface visibly disturbed, wide slow concentric ripples rolling outward across it and the amber lamplight on it broken into long wavering streaks between the rings", "phrase": "the black floodwater on the left rippling in slow wide rings, the broken amber reflections on it wavering and sliding", "still_as": "the bronze flood door, the stone steps and the carved piers"}}
WHY: The only mover in this spec is in an old-style `animate` block, which the loop's mover check (`_has_authored_mover`) can't see, so the loop treats the room as having no mover. Adding a `motion` block to the same bounded pool on the left fixes that: the pool gets a visibly rippled surface in the still and a matching short motion prompt. The runner matches this to the existing `still_flood` element by its `at` value ("to the left" hits `still_flood` before `flood_door`) and merges rather than replaces, so the old `animate` block stays for the player.
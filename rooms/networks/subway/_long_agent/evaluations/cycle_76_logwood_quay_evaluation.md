I opened `l1_logwood_quay_1.png` (3072×1024) and cut native-resolution crops at the blue unit's base (768–1408), the green unit's base (1600–2240) and the pump/left tunnel (256–896), rather than relying on the worker's account. The named defect is real and exactly as described: in both train crops the solebar rests directly on the same flat wet flagstone deck the viewer stands on, with a rail inlaid flush like a tramline and no waist-deep bed, no wheels below platform level — and the pump crop independently shows the flush rails running away down the paved deck into the tunnel mouth, so all four door hotspots really would box vehicles parked like huts on the walkway. I also confirmed the sump pump is well-formed and boxable (bolted casing, ribbed hose, logbook and pencil, caged lamp), so the checklist object the worker credited is genuinely there. The SPEC is a true single-variable change: I diffed it against `scenario.json` rooms[6].authoring.sceneSpec and `seam`, `seamOccluder`, all ten elements (both clue and all four door assignments intact), `atmosphere`, `negatives` and `continuity` are untouched — only the `setting` line gains the raised-walkway/sunken-bed clause, with its original tail preserved. Attempt history holds only the initial generate, so this is not a repeat.

VERDICT: PASS
I read the four authority files. Here's where this stands.

The gate report on cycle 19's render clears every gate: camera `still`, glitch breadth 0.104 raw / 0.042 normalised (both far under the 0.70-on-both threshold), dead gate `false` with a wander ratio of 12.06 against a 1.75 floor and a peak tile difference of 7.58 against 0.45 — so there is real, localised motion in the frame, not a frozen render. Return is 0.08: low, but not the zero that would escalate, and subtle is explicitly enough (market_price returns 0.23 and ships). Peak 8.04 sits right in the band crown measured flat across (8.18 / 8.39 / 8.01), so there's no evidence of "too lively" to act on, and nothing in the report says a non-mover is moving.

The spec already declares exactly one bounded mover — `hide_rail`'s row of hanging canvas sheets, hung singly with air behind each, with vigour authored into `motion.vigour` and a matching `motion.phrase`. The motion prompt resolves from that spec cleanly, names only that mover, and is 38 words in the house form. Nothing to re-author, nothing to soften, nothing to pin, and a clip now exists, so RENDER is spent.

No metric may approve a clip, so the terminal state for a survivor is to queue Lucas's eye.

DECISION: HOLD
WHY: The clip cleared all four reject-only gates — camera still, glitch well under threshold on both readings, and the dead gate explicitly negative with a 12.06 wander ratio — on a spec carrying exactly one bounded authored mover, so there is nothing left for this loop to change and the clip goes to Lucas's eye.
I verified the chain independently rather than trust the write-up. I cropped the `village_lamps` box (338×103 at `[0.46,0.49,0.57,0.59]`) from shipped frames 1/31/59 at 4× and the aim is correct — three warm village clusters plus a lit strip sit inside it — and my own `measure_subjects` run reproduces the worker's numbers exactly (shipped box p95 2.77, 276 lamp pixels at luma>80 = **0.79%** of 34,814, lamp-pixel tstd p95 8.09, dark-flat p95 2.66; base `_src` 4.23 with lamp p95 14.16). That fraction is the whole argument and it holds: at 0.87% coverage the lamps sit above the 98th percentile, so the p95 the gate reads is *definitionally* the flats, and no lamp brightness or tighter box can move it — which is why the already-applied `village_lamps` tile only made it worse (4.23 → 3.18 → 2.77), and why another tile or `force_repair` is the wrong instrument here despite the usual rule. The proposed action is the only remaining lever and it is properly scoped: `rigid` drops "flats" (the exact band declared frozen), the mover is recast as haze — a category `STABILISER_TAIL` explicitly permits, unlike bare lamplight — names and both boxes are byte-identical, `river_glimmer` is untouched so the change is attributable to one subject, the new pinned line and "fog bank / thick mist / obscuring haze / lights going out" negatives are guardrails on that same change rather than scattershot, `validate` returns clean, and it does not repeat attempt 1's re-aim-and-rewrite. One thing the runner should carry: the `village_lamps` entry in `cine_night.patches.json` is still `enabled` and demonstrably subtracted ~1.05 p95 from this box, so it should be disabled for this rerender or the haze gain will be partly eaten again.

VERDICT: PASS
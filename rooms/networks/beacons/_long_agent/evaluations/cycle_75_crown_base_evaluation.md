I extracted all 61 frames and built an independent max–min diff map. The billow is real and exactly as described: frame 0029 shows the linen sheet reared into a sail across the valley with the old still box visible as a hard rectangular notch clipping its lower-right corner, and the strong-motion blob (>80) sits at x 0.40–0.94 / y 0.40–1.00 with **95.2% of its pixels inside the proposed (0.545, 0.50, 0.685, 1.00) box** — only 1 stray pixel in 0.545–0.60 and 22 in 0.685–0.72, the remaining ~2.5k being the louvre shimmer at x 0.72–0.80 that the worker explicitly deferred. The old box measures mean 1.03 (pin works, aimed too low), while `trough_river`'s p95 of 32 decomposes into mean 15.7 left of x 0.543 versus mean 109 right of 0.548 — the sheet, not water — and a native crop confirms the last river channel ends near x 0.515, so the new left edge lands on dry scree and freezes no live water.

I also checked the tier objection from the last cycle and the worker is right: `apply_rebake` (cine_iterate.py:199) takes only `mask_pct`/`mask_region`/`drop_patches` and never touches scenario.json, `parse_cine_decision` (long_agent.py:1657) attaches a SPEC only for RERENDER/AUTHOR, and there is no `cine_base.patches.json` in the room — so "write_spec + tier-1 REBAKE" is not expressible in this runner, and mask percentile is the wrong lever since the sheet is ~10× the river's amplitude. The change is one geometry edit to one subject with all other names, boxes and phrases preserved, it is not a repeat of attempt 1 (stale-art rerender), and the prompt-side pin/negatives are reinforcement for the same single defect.

VERDICT: PASS
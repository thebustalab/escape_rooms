I extracted all 61 frames and built my own max–min diff map: the diagnosis is real and the numbers are honest — the linen sheet visibly rears off the slab in frames ~16–31 and settles by ~46, and the dominant motion component sits at x 0.547–0.661, y 0.526–0.862 (worker said 0.545–0.670 / 0.520–0.865), with the old still box showing as the hard black rectangle clipping its lower-left corner. But the tier is wrong: the worker's own stated mechanism ("the box edges land on static rock, so **the bake** freezes the sheet deterministically") is bake-time still-pinning, and `cine_scenario._still_boxes` resolves pins from scenario.json inside `_mask_inputs`, which `rebuild_clip` calls — so `write_spec` + tier-1 REBAKE off the existing `cine_base_src.mp4` fixes this with no GPU, and the current clip's frozen rectangle is itself proof the pin mechanism works, just aimed too low. A GPU re-render buys nothing here by the worker's own admission that the prompt side is maxed out, and it re-rolls a render that is otherwise fine on every metric. The `trough_river` shrink is also an unforced second subject change: the freeze wins over the mask regardless, the region it would swallow (x 0.547–0.59) is dry gravel with no visible water, and `p95` is driven by the live channels further left — so leave that box alone and keep this attempt attributable to the one geometry change. Re-submit as REBAKE with only the `slab_sheet` box enlarged (the added still *phrase* is fine — `motion_spec` treats a still subject's phrase as a pin, not a mover).

VERDICT: FAIL
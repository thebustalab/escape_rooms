I verified independently rather than trusting the write-up. Frame 0 of `cine_night.mp4` is a clean night panorama of a broad braided-river valley with a **lit** fire-basket, while `scene_night.png` (Sep 4 06:42, two days newer than the Sep 2 15:49 bake) is the cirque composition — black tarn, horned outcrop, cold empty basket — so `frame0_corr = 0.117` is stale art, not a noise render, and rebake is correctly ruled out since `cine_night_raw/src.mp4` are also Sep 2. Native crops confirm the re-aim: the old `village_lamps` box [0.3,0.5,0.4,0.6] now covers only dry scree with zero lamps (the worker said "tarn water", a slight mis-description of the same dead region), the new [0.46,0.49,0.57,0.59] sits squarely on the scattered warm village points, and the new `river_glimmer` box lands on the ice-laced tarn rim and glittering wet runnel while staying clear of the rigid spyglass and tripod. Subject names are preserved, this is attempt 0, and the identical diagnosis on this room's `base` state last cycle took `frame0_corr` 0.04 → 0.921 with a `hold` verdict, so the tier and mechanism are proven here; the multi-field spec rewrite is one attributable change (re-target to replaced art), not a reshuffle. One thing to watch on the re-judge: two goats sit inside the new `river_glimmer` box at roughly x 0.53 / y 0.63, and unlike the base spec the night `pinned` list has no line holding the animals motionless — add one if the next clip shows them stirring.

VERDICT: PASS
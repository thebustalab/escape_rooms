I pulled the 61 frames and ran my own sky cross-correlation: the diagnosis is real — a sky patch at (1200-1900, 60-300) holds zero-shift corr 1.00 at f0001, collapses to 0.18 by f0016, and translates coherently to dx=+14 then swings to dx=-14 by f0026 before recentring at f0061, which is exactly the drift the pinned line forbids and which the gates can't convict on because `rigid` names only ground furniture.

But the tier is wrong. `cine_scenario.mask_png` applies `still_boxes` as a hard zero **after** threshold, region cut and feather (lines 114-131), and `apply_rebake` → `rebuild_clip` → `_mask_inputs` → `_still_boxes` reads them straight from the spec — so writing the two `still: true` boxes and REBAKING freezes the sky against the existing render for zero GPU, with no bleed risk at the 0.47/0.48 edge because the zero is unfeathered. Your own attempt-2 evidence proves the render doesn't need touching: `cine_night_src.mp4` is byte-identical (md5 0b354e13…) to its pre-rerender copy, so the drift lives in footage a rebake can mask, not in footage a rerender must replace.

The rerender also bundles a second, unattributable change — adding the sky to `rigid` — when the prompt *already* carries "the stars are fixed … does not rotate, drift or streak" plus negatives "star trails, rotating sky, moving stars" and the model ignored all of it; more of the same wording is the reshuffle the rule forbids, and it breaks the seed-reproducibility that is currently keeping `village_lamps` and `river_glimmer` alive. Re-emit as REBAKE with the two still boxes as the single change, leaving `rigid`, `pinned`, `negatives` and both mover boxes untouched.

VERDICT: FAIL
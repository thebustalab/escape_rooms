I verified independently rather than trusting the write-up. Extracting frames 0/15/30 from `cine_night.mp4` (3072×1024) and cropping at native resolution: the current `village_lamps` box [0.3,0.5,0.5,0.6] lands on the kiln dome's stonework and the chimney shoulder, and `river_glimmer` [0.4,0.6,0.6,0.7] on more dome rock plus the cold fire-basket staves — no lamps, no channels in either. The proposed boxes are right: [0.58,0.48,0.79,0.57] contains the four warm village clusters on the benches and [0.63,0.61,0.79,0.79] contains the braided pale channels, both plainly visible in my own crops. RERENDER is also the correct tier despite the seed reproducing the footage, because the runner only calls `write_spec` on the RERENDER/AUTHOR branches — a REBAKE block has no SPEC field, so a box re-aim is not enactable through the cheap tier, and `_record_patches` rewrites `cine_night.patches.json` from the new render, clearing the enabled tile still aimed at [0.3,0.5,0.5,0.6]. Two boxes move, but that is one change of one kind with one cause, it repeats nothing from attempt 1 (stale-art rerender), and the phrases, rigid, pinned and negatives are byte-identical, so a subsequent failure stays attributable.

VERDICT: PASS
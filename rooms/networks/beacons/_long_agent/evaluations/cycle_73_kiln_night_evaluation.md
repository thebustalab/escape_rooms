I reproduced the diagnosis independently on the source frames: the sky patch (1240–1860, 60–300) translates coherently from (0,0) to (+25,+9) by f0036, snaps to (−18,−5) at f0041 and recentres by f0071, while the control ground patch (300–900, 760–980) holds (0,0) at corr ≥0.966 throughout — a real violation of the "stars are fixed" pin that `landmark_drift_px` structurally cannot see. The tier is right: `cine_night_src.mp4` (md5 0b354e13…) is byte-identical to its pre-rerender copy, so the drift is in footage a mask can freeze, and `cine_scenario.mask_png` applies `still_boxes` as an unfeathered hard zero after threshold/region/feather — the same mechanism that already worked on `hood`, whose `cine_night.mask.json` records `star_field` pinned with `village_lamps` and `river_glimmer` still alive. The box is safe and attributable: I found zero warm lamp pixels above y=0.48 (warm rows span 0.518–0.794), so the [0,0,1,0.46] band holds only sky, snowfields, ridgelines and dome rock, and the tstd profile (4.1–5.4 in rows 0.00–0.30 vs 1.5–1.7 at the subjects) confirms no slider could have separated them. The written spec preserves both existing subject names, boxes and phrases, adds one phrase-less `still` subject, and `render_prompt`/`render_negative` compare byte-identical with and without it, so the fixed-seed render is untouched and the change is a single attributable one.

VERDICT: PASS
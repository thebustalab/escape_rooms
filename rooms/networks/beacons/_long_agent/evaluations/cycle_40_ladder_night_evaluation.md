I reproduced every claim independently. Native crops of frame 0001 show the old `village_lamps`/`river_glimmer` boxes sit on black headwall rock (`unlit`, `lit_pct 0.04 / 0.00`, `dead`), while the proposed boxes land squarely on the lamp string and the braided channels and measure `alive, p95 5.03 / 4.76, p95_lit 17.54 / 20.07` on the *shipped* clip — so the subjects were never dead, the boxes were mis-aimed, and the box change is the fix rather than a silent re-measurement. The fire claim also holds: `scene_night.png` has 0 px >150 in the basket region, the clip has 0 at frame 1, 2892 by frame 31 (the crop shows open flame lighting the cordwood) and 2 by frame 61 — a render-time ignition that breaks the existing COLD pin, which you correctly suppress with negatives and a `still` witness instead of legalising, exactly as the last round's feedback directed. The `still` fire_basket witness measures `p95 49.4, hot: true` on the current clip, so a recurrence will hard-reject instead of sitting in the warn band. The two changes touch disjoint measurements (subject boxes vs. background swing), so the loop can still attribute the outcome, and rerender is the right tier because the box fix alone would ship a clip that still ignites.

VERDICT: PASS
I extracted the frames and checked both claims independently.

Verified: frame 55 shows a full flame burning in the "cold, empty, unlit" fire-basket with a second glow on the spyglass objective, and frame 1 is dark — so the diagnosis is grounded in the frames, not in the judge's numbers, and the rerender tier is right (no rebake can subtract an invented light source). Verified: the old `river_glimmer` box [0.4,0.4,0.6,0.5] crops almost pure star field with only a sliver of ridge, the proposed box lands on the braided channels, and re-measuring the shipped clip gives p95 3.57 — and 3.91 with the `village_lamps` overlap excluded, so the "alive" reading is the river, not lamp flicker leaking in. I confirmed the polarity claim at the source: `motion_spec.py:166-171` splices `pinned` into the positive body, so "no flame, no ember, no glow, and no fire" was conditioning the positive prompt while the negatives carried only "fire spreading". One caveat to carry forward: the same lines show `still` phrases are appended to that identical positive list, so the new `fire_basket` pin ("...casting no light on the stone") and `spyglass` pin ("...nothing bright at either end of the tube") put fire/light nouns and negations back into the positive prompt at exactly the two spots that ignited — if the flame recurs, strip those two phrases to purely affirmative wording ("dark rusted bars", "dull dark metal") before changing anything else, since the negatives are the lever doing the real work here.

VERDICT: PASS
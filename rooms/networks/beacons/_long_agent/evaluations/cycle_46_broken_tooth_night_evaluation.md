I verified this independently rather than trusting the write-up. My own native-res crops of frame 0 and frame 30 confirm the claims exactly: `river_glimmer` [0.4,0.5,0.6,0.6] holds only the dark cliff face and the rusted fire-basket bars (no water anywhere), the old `village_lamps` box holds the basket and foreground rock, and the proposed band [0.35,0.425,0.77,0.5] sits squarely on the lit village windows, several of which visibly extinguish and relight between the two frames — and the room's own scene negatives do say verbatim that "the main valley floor, its braided river and its low benches are NOT visible from here at all", so `river_glimmer` names something the art is forbidden to contain.

My own measurement on the shipped clip reproduces the worker's numbers (corrected band p95 9.33 vs their 9.30, above WEAK_P95 8.0 → alive; the old boxes give 4.44/6.86), so the 4.49/6.94 rejection really was an artefact of measuring rigid rock, not a bad render. `still: true` is a first-class field in `motion_spec.py:122`, and at p95 4.44 the pinned `river_glimmer` sits well under the 8.0 still-violation threshold, so the pin will not immediately trip. The change touches two subjects, but it is one attributable diagnosis (both boxes mislocated, one naming an impossible object), it leaves exactly one live subject to attribute the next result to, it repeats nothing from attempt 1's stale-art rerender, and PASS would be wrong here because it would leave the broken boxes to fail forever.

VERDICT: PASS
I pulled the frames myself and every load-bearing claim holds.

The star swarm is real and I reproduced it independently on the pre-rebake shipped clip: sky (y<0.38) temporal std mean 3.65 / p95 12.2 with 38% of pixels >3, against 0.99 below the ridges, and a 4× zoom on frames 1/11/21/31 (/tmp/gk/starzoom_pre.png) shows the star pattern completely reshuffling with comet-dashes — a flat breach of the "stars are fixed… no star trails" pin that no gate can see. The tier is right and I verified it in code, not just by assertion: `mask_png` zeros still boxes last (cine_scenario.py:115-131), `_still_boxes` resolves them from `scenario.json` (cine_scenario.py:191), and `rebuild_clip` routes through `_mask_inputs` (cine_scenario.py:287), so this needs no GPU — and `movers = [x for x in spec["subjects"] if not x.get("still")]` (_motion_specs.py:436) confirms the pin never reaches the prompt, so the byte-identical-render-prompt claim is structurally true. The spec change is exactly one attributable edit: `star_field` added, both existing subjects' names, boxes and phrases untouched, and the box move correctly deferred as the last feedback instructed. Post-rebake the fix is clean and cost-free — sky std 0.02, ridge and lower-frame stats unchanged to two decimals, subject lit-pixel std identical pre→post (lamps 2.49→2.52, river 4.80→4.82) with the amplified diff (/tmp/gk/lamps.png) lighting up precisely on the braids and amber lamps, both patch tiles preserved, and the pin boundary invisible at 1× (only a hard edge at 12× against codec noise on the spec-rigid snowfields).

VERDICT: PASS
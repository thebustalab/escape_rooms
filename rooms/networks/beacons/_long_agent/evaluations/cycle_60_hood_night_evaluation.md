I pulled the frames myself. The diagnosis is right and I confirmed it independently on the **shipped** `cine_night.mp4` (not just the raw): a grain-extract diff of the sky between frames 0 and 20 (/tmp/cine_check/sky_diff.png) is a dense field of black/white star dipoles across the whole sky, and per-region temporal std gives sky mean 3.40 / p95 11.0 / 39% of pixels >3 against valley mean 0.68 — the stars really are swarming and it really does break the pinned "stars are fixed". The box move is also right: cropping [0.3,0.4,0.5,0.5] shows bare ridgeline and cloud sea with no lamps, while [0.32,0.56,0.63,0.72] lands squarely on the lit village and braids.

But the tier is wrong, and by the worker's own mechanism. `cine_scenario._still_boxes` resolves `still: true` subjects from `scenario.json` at BAKE time and `mask_png` forces those boxes black last, after threshold, region cut and feather — and `rebuild_clip` (the rebake tier) calls `_mask_inputs`, so the pin lands on a rebake with no GPU at all; `cine_night_src.mp4` is on disk to rebuild from, and I measured the wanted subjects already alive in this render on the corrected boxes (lamps lit-pixel mean tstd 5.9 / p95 21.0, river 6.5 / 21.2), so there is nothing a new render needs to create. Call `write_spec` with this spec (the star_field pin is the one attributable change — leave the box move for a separate pass or state plainly that it only affects measurement, not the bake) and then `apply_rebake`, escalating to RERENDER only if the pinned rebake still fails.

VERDICT: FAIL
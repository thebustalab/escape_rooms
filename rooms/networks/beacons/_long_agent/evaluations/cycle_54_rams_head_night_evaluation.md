I extracted the frames myself and the worker's diagnosis holds exactly. In `cine_night.mp4` the fire-basket region (x 0.74–0.88, y 0.50–0.72) goes from mean 19.4 / max 207 and blue-dominant at frame 1 to mean 39.0 / max 255 with a warm p99 of R221 G178 B133 at frame 52 — the native crops show a black empty basket becoming a burning fire that lights the plinth and fuel-store, and whole-frame mean 14.8 → 18.6 accounts for the reported `background_swing` of 3.67. The claim that this is new in attempt 2 also checks out: `scene_night.png`'s basket peaks at 166 and the attempt-1 clip is flat at 17.3–17.8 across all 61 frames, so the regression is attributable to the "dim and brighten and waver" clause attempt 2 introduced — a direct breach of the existing pinned line, which is an objective violation rather than the human judgement call the `hold` tier implies, so RERENDER over PARK is right. The spec is a real causal edit, not adjective reshuffling: the brightness-modulation clause is replaced by occlusion-driven variation so the haze mechanic that made the box alive survives, backed by a hard basket pin, a global no-brightening pin, and plain fire/flame/firelight negatives the list has never carried. Names and both boxes are byte-identical, `river_glimmer` is untouched, and while the change spans phrase, pinned and negatives it is one attributable intervention against one diagnosed cause.

VERDICT: PASS
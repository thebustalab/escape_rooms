I extracted all 61 baked frames and checked this myself rather than taking your numbers: f0009 and f0054 across the cloud_sea box are visually identical at native resolution — every lobe, hole and tongue over the village island sits in the same place — the ×14 diff is dark over the cloud body and bright only on village stone and ridge texture, and tile block-matching finds zero displacement anywhere in the box, so your dead-not-weak call on cloud_sea is right and the judge's p95 6.62 is grain on high-frequency detail inside a very large box. Your pre-bake check also holds where it matters: in `cine_base_src.mp4` (73 frames) I measure cloud core 4.41 against flagstone 5.13 and hood-rock 3.68 while ridge_grass reaches 9.06, so the renderer never made cloud motion and the mask did not kill it — a rebake was correctly ruled out. The action is the cheapest sufficient one and fully attributable: I diffed your spec against the current one field by field and the only difference is `force_repair: true` on cloud_sea, with names, boxes, phrases, rigid, pinned and negatives byte-identical, and `motion_spec.repair_list` does put a forced subject on the list despite its "weak" verdict. It is not a repeat of attempt 1 (a deterministic stale-art rerender), and force_repair is the sanctioned handling for a dead subject rather than rewording. One calibration note for next time: your rigid controls ("cowl 3.40, flagstone 3.53" ≈ cloud 3.57) overstate the case — with my control boxes the σ5 envelope is 1.70/2.46 rigid against 3.52 for cloud, so lean on the frame-pair and src evidence, which is what actually carries this.

VERDICT: PASS
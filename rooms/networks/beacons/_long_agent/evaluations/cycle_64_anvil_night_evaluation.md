I verified the worker's claims independently rather than trusting them.

I extracted the frames myself: the tableland brightening is real and visible A/B (fg pavement 44.3→53.2→45.9, mid pavement 58.6→68.7→61.0), the fire-basket is genuinely unlit so attempt 2's polarity fix held, and I reproduced the worker's exact raw-render figure (`cine_night_raw.mp4` whole-frame swing 4.02), confirming the defect is in the render and not the bake. The impossibility argument for masking checks out by an independent route: per-pixel temporal std is 5.2–7.5 on the pavement against 1.1 median in `village_lamps`/`river_glimmer`, so at global p93 roughly 20–27% of pavement pixels still pass the mask while only 3.5–4% of lamp pixels do — any threshold tight enough to kill the swing kills the subjects several stops earlier, and `region` in `auto_mask.py` is a min-region-size cut, not a spatial band, so there is no way to target the pavement spatially either. I also ran `colour_normalise.py` on the raw clip myself: whole-frame swing 4.02→0.63, pavement 11.77→2.12 — the fix is real, and my grep confirms it is orphaned, with `apply_rebake` exposing only `mask_pct`/`mask_region`/`drop_patches` and no hook for it. The `fire_basket`/`spyglass` "dead" verdicts are correct by design (both are in the mask's `stillBoxes`), and the spec's `pinned[3]` plus the negatives already state the illumination pin as strongly as it can be stated, so a third rerender really would be re-asserting a spent lever. One minor overstatement: the "same gain on three widely separated regions" is not literally global — a dark left-hand band reads ~1.02 and the worker's own sky figure is 1.04 — but a per-frame gain normalisation demonstrably removes it, so the conclusion stands.

VERDICT: PASS
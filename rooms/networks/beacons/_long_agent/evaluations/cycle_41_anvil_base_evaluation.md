I verified the claims independently rather than trusting the write-up. Cropping the far_smoke box at native resolution (x1597–1813, y348–553) across the loop confirms the diagnosis: the plume silhouette is identical panel to panel with no climb, upper-plume temporal range is 1.30 in the bake and 4.24 in the raw (consecutive-frame diff p95 of 1 and 2), so motion lives only at the root and the ridgeline — the render never animated it and no rebake could, making RERENDER the right tier; the art is also plainly a fat column, not "hair-thin", so the phrase rewrite is a real correction and not an adjective reshuffle. `motion_spec.render_prompt` (lines 166–171) does append pinned lines verbatim into the positive body, and I confirmed the current composed prompt asks for a smoke column and then says "no smoke rises from it" two sentences later; `force_repair` is a real honored key, the proposed spec validates to `[]`, names and boxes are unchanged, only far_smoke is touched, and this repeats nothing from attempt 1 (a deterministic stale-art rerender).

One attribution caveat for the next cycle: `fenwatch` and `whistlegate` carry the identical PIN_BASKET clause and their smoke subjects animate cleanly at 10.22 and 11.08, so the pin contradiction is not sufficient on its own to kill a plume — the static verb "standing" plus the "hair-thin/thins" mismatch against fat art is probably the stronger lever. Since both edits serve one hypothesis on one subject and the pin edit is conservative (basket stays cold, fire negatives untouched, and the change is scoped to anvil's scenario.json, not the shared constant), this is still attributable enough to act on; if it fails, drop the pin edit as the variable to re-test rather than rewriting the phrase again.

VERDICT: PASS
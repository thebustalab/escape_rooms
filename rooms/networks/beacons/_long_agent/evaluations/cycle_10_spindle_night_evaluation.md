I extracted all 61 frames myself and re-implemented the judge's actual metric (temporal std over 16 sampled frames, `motion_spec.py:174-190`), reproducing 3.44/3.52 for the two boxes, and the worker's diagnosis holds: the motion is exclusively on the lit pixels — pixels with tstd>8 inside the `village_lamps` box have mean luma 79.8 against a box mean of 12.0, 92.8% of them are luma>40 and *none* is below luma 15, and the amplified variance map traces the braided channels and lamp points rather than a diffuse noise field. Against luma-matched rigid regions (the pinned snow slopes at luma 40–100 measure tstd p95 5.2–6.3) the subject pixels run 7.2–9.9, so the motion is real and above the noise floor, not codec shimmer. One correction to your write-up: "p95 range 33 against a 4.5 bar" compares max−min range to a bar defined on temporal std — in the judge's own units the lit-pixel p95 is ~9–11, i.e. 2–2.5× the bar, still passing but a much thinner margin than you claimed, so state it in the judge's units next time. I also checked the pre-bake `cine_night_raw.mp4` (4.14/4.36 whole-box, 11.3 on lit pixels), which confirms the judge was right to call this truly_dead rather than mask-killed and correctly rules out a rebake/mask-widen as the cheap fix — leaving no attributable lever for a third rerender, since boxes and phrases are already verified correct and `force_repair` tiles are already applied and made the whole-box number worse. PASS is the right call; the shortfall is box geometry (~97% unlit rock), not missing motion.

VERDICT: PASS
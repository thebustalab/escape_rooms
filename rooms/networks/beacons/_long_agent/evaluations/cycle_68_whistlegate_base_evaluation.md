I pulled my own native crops of the proposed box at f1/13/25/43 and a 4× zoom on the T-stand: the object does **not** change identity the way you describe — it reads as the same cross-armed bracket in all four — so that part of your diagnosis is overstated and you should describe what you see more conservatively next time. The substance holds independently, though: my own per-tile temporal-std sweep makes x=736,y=576 the single hottest 32×32 tile in the panorama at 15.15 (your 15.05), the residual survives per-frame brightness normalisation (8.18 → 8.17, so it is not firelight flicker), and f25 vs f36 shows real structural churn on the bracket — a rigid prop that should be dead. The mechanism checks out on disk: `cine_scenario._still_boxes('.','whistlegate','base')` now returns `desk_ledger` **and** `bench_scales` [0.23,0.5,0.33,0.78], `cine_base.mask.json` has `enabled: true`, `cine_base_src.mp4` is present, and `apply_rebake` → `rebuild_clip` → `_mask_inputs` zeroes the box after threshold/region/feather — so the rebake will deterministically freeze it for no GPU. The change is single and attributable (one new `still` subject; every existing name, box and phrase untouched, `gate_daylight` clears at 0.22 < 0.23, `states.night` preserved), it is not a repeat of a prior attempt, and it is the tier the last review explicitly asked for. Note that this does not touch the only tiered finding (`weak_subjects` → `hearth_fire`, tier `human`), which correctly stays for Lucas's eye.

VERDICT: PASS
I reproduced the worker's measurements independently rather than trusting them. The spec box `[0.12,0.28,0.14,0.32]` is native pixels (369,287)–(430,328): my crop shows nothing but two fixed stars and compression noise, mean luma 1.56 with inter-frame |diff| p95 of **0.00** — there is no fire in it to animate, so attempt 1's rerender could never have moved that number. The real ridge-shoulder beacon sits at x 0.158–0.164, y 0.438–0.458 (found by an independent warm-blob scan, not by taking the worker's coordinates), inside their proposed `[0.148,0.415,0.176,0.483]`, with motion_mean 20.9 and a tiled native crop of f0001/0016/0031/0046 showing the flame visibly guttering and changing silhouette — alive, on par with `summit_brazier` (diff p95 12.0 vs 10.0). The background_swing warn is also as described: global luma runs 13.67 at f0003 to 21.65 at f0033, matching the worker's figures exactly, and every warm source keeps a brightest pixel ≥157 for all 61 frames, so nothing goes out — and my sweep found **zero** dead warm sources anywhere in the panorama, so no second beacon is hiding behind the plural in the phrase. One gap, not disqualifying: the decision is a bare PASS, so the corrected box is only mentioned in prose and the same false `dead_subjects` reject will fire on the next judge pass — the box move should be recorded as the spec edit accompanying this pass.

VERDICT: PASS
I pulled native crops of the proposed box (x 706–1013, y 512–799) at frames 0, 12, 24, 42 — the morphing is real and I confirmed it independently: the iron stand behind the scale beam is cross-armed at f0, a shallow stemmed bowl at f12, cross-armed again at f24, and a knobbed top at f42, with the scale beam and pans otherwise static, so this is latent boiling on a rigid object, the box sits squarely on it, and it clears `gate_daylight` (0.22 < 0.23) with every existing name, box and phrase untouched. The tier is what's wrong. Your own mechanism argument is a *bake-time* one: `cine_scenario.mask_png` applies `still_boxes` last, after the threshold, region cut and feather, and `_still_boxes` re-reads `scenario.json` on **every** path to a bake — its docstring says so explicitly ("including the gallery's commit mask + re-bake … a pin that survived a render but was dropped by a re-bake would be worse than no pin at all"), and `apply_rebake` → `rebuild_clip` → `_mask_inputs` goes straight through it, with `cine_base_src.mp4` present. So `write_spec` with this exact `bench_scales` subject followed by REBAKE freezes the lamp-stand deterministically against the existing render for no GPU, whereas RERENDER re-rolls the fire, smoke and daylight — all currently passing — for nothing but a prompt-side pin you don't need; resubmit the identical spec as REBAKE.

VERDICT: FAIL
Verified independently rather than trusting the description. Frame 0 of `cine_base.mp4` correlates 0.997 with `scene_undo0.png` and only 0.31 with the committed `scene.png`, while f0↔f35 correlate 0.999 — so the clip is a coherent render of superseded art, not noise, exactly as the worker diagnosed; the visual check confirms it (old art: broad walled platform, stone fuel-store hut, low stump; current art: narrow rock fin, tall needle with a spindrift plume, snow-slopes to both edges). Cropping the `needle_spindrift` box (737–1136 × 30–327) out of `scene.png` lands squarely on the needle and its plume, and out of the clip lands on empty sky, which fully accounts for the "dead subject" reading without needing force_repair or a tile. RERENDER is the correct and cheapest sufficient tier — a rebake would only re-apply patches to the same stale render — and I diffed the proposed spec against the current one as parsed JSON: byte-for-byte identical, so no subject name, box, or phrase was silently altered and nothing scattershot was smuggled in. The unaddressed `survey_sheet` still_violation is moot, since that box also sits on stale art and its measurement resets against the correct still.

VERDICT: PASS
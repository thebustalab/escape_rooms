I pulled frame 0 and frame 40 myself and compared them against the current `scene.png`: the clip is a clean, well-formed render of the *old* rams_head art (open valley, braided glacial river, parapet terrace, hanging glacier), while `scene.png` (mtime Sep 2 22:32, 7 h after the Sep 2 15:26–15:28 bake) is the new cirque art — dark back-wall, horned outcrop, ice-rimmed black tarn, goats — so the worker's "stale art, not a noise render" diagnosis is correct and `frame0_corr` 0.04 plus every subject/still reading in the judge report is measuring unrelated pixels. RERENDER is the only tier that can fix this; a rebake would just re-derive a mask from the same superseded render. I cropped the spec boxes against the new art: `tarn_surface`, `horn_meltwater` (wet streaked rock under the horn) and `survey_sheet` (sheet under its stone weight) all land correctly, and the old `gap_grass` box [0.6,0.52,0.73,0.66] visibly contains the brass spyglass barrel at its left edge — a body the spec declares rigid — so the shift to [0.65,0.52,0.74,0.64] is a verified necessary correction, not a silent reframe, and it is the only subject touched. Attributability is not compromised here because no prior measurement was valid to begin with, and the history is empty so nothing is repeated. One thing to watch next cycle, not a blocker: the new `gap_grass` box is largely scree with only sparse grass tufts, so if it comes back reading dead the box (not the wording) is the thing to move.

VERDICT: PASS
I verified independently rather than trusting the write-up. Extracting all 61 frames and recomputing the temporal-σ stack reproduces the worker's numbers exactly (old boxes p95 4.41/4.12, `p95_lit` 10.98/39.36; corrected boxes 34.27/34.25), the sparse-lit rescue at `motion_spec.py:213-226` is genuinely present with `SPARSE_FRAC` 0.05, and the on-disk `cine_night.judge.json` at the identical `clip_mtime` 1788660135.94 now reads **hold** with both subjects `alive` — the reject JSON in the brief carries no `p95_lit`/`lit_pct`/`unlit` keys, so it is provably pre-rescue and stale. Native-res crops confirm the motion is real and on the subjects: between frames 0001 and 0031 the warm lamp points and pale braided channels visibly brighten and shift (lit-pixel count 194→562 in the village cluster, river max luma 204→237) while the building silhouettes and ridge geometry are pixel-identical, consistent with `landmark_drift_px` 0 and `rigid_frac` 0.95. The box correction is verified and worth landing: the old `village_lamps` box (px 921–1536 × 409–512) is moonlit snowfield and cloud, and — a point the worker missed — every one of its 105 "lit" pixels sits in rows y409–415 at the top edge, i.e. the rescue there is measuring pinned snowfield, so that box is `alive` for the wrong reason, whereas the corrected box demonstrably contains the lamps. A GPU rerender for a clip that passes the current judge would be waste; land the two box corrections as spec hygiene only, and note in the spec log that `village_lamps`'s pre-correction rescue was reading a rigid pin.

VERDICT: PASS
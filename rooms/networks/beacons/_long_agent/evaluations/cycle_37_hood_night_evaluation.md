The box finding is real and I confirmed it myself — the old `village_lamps` box (px 921–1536 × 409–512) is pure ridgeline and cloud with zero lamps in it, and my own 12×6 heatmap corroborates the dead reading (valley p95 4–5 against sky p95 75) — so keep the corrected boxes. But `force_repair` is a no-op here: `repair_list` (motion_spec.py:210-212) already returns any subject with `verdict == "dead"`, both subjects were dead, and `cine_night.patches.json` plus `_scratch/tiles/hood_night_{village_lamps,river_glimmer}.mp4` prove the last render already gave each one its own tile and composited it — so the "WHY" rests on a mechanism that was already on and already failed. Worse, the diagnosed cause is contradicted by the frames: I measured the existing isolated tiles at p95 8.0 (lamps) and 10.0 (river), and those tiles contain no sky at all, so the starfield is not eating the motion budget — the tile render itself comes out quiet and the composite + crossfade bake then drags it to 4.42/4.18 against DEAD_P95 4.5. Make one attributable change with a mechanism that is not already running — the boxes are near-miss (tiles at 8–10 landing at 4.2–4.4, so only a modest gain is needed), which points at the tile render's own phrasing/amplitude for these two subjects, not at re-flagging repair. Also, do not move both boxes and re-flag both subjects in one attempt; land the verified box correction alone, or the box correction plus exactly one mechanism change, so the next judge run is attributable.

VERDICT: FAIL
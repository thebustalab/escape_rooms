Read all four sources. Findings for `sisters`:

- **The spec already carries a bounded mover.** `col_cloud` has a full `motion` block (`moves`/`vigour`/`phrase`), so `_has_authored_mover` sees it — this room was in the 2026-09-13 migration batch and is not invisible to the loop.
- **It is bounded, and that is measured, not assumed.** The Sep-7 judge localized `cloud_river` to box `[0.44, 0.24, 0.64, 0.66]` — ~20% of width, ~42% of height, one discrete feature in the throat of the notch. This is not hood's frame-filling cloud sea; it is the shape that survives at 3072. That judge scored it `alive`, p95 11.55, on this same committed `scene.png`.
- **The ledger's "sisters is dead" line is retracted in the ledger itself** (2026-09-10): *"`sisters` is NOT a dead room. Its cloud cascade animates… Lucas: 'first time those clouds have moved'."* No reason to re-author.
- **The motion prompt is already the spec-derived one** — 38 words, house form, names only the depicted mover, and matches `col_cloud.motion.phrase` verbatim. No rigid clause active, which is right: the stored `motionSpec.rigid` is a 7-item inventory plus 4 pinned sentences, exactly the a_opera bloat that renders CAMERA MOVES, and it must stay switched off unless a gate report demands it.
- **The still is committed, human-accepted and seam-clean** — `accepted: true` (Lucas 2026-09-03), `seamBandRun 0.012`, screened 2026-09-14.

Gate report is empty and nothing has been tried, so there is nothing to judge and nothing to fix. The room is authored; it needs its one render.

DECISION: RENDER
WHY: The spec carries a bounded, vigour-authored mover that has already measured alive on this exact committed still, the motion prompt derives correctly from that mover's phrase, and no clip exists in this loop yet — so the single render at the pinned settings is the next step.

One thing worth Lucas's eye, not actionable this cycle: `sisters` is one of the two committed rooms whose cached `authoring.scenePrompt` **predates the EQUIRECT clause** (801 words, no `EQUIRECTANGULAR` string). I did not act on it — the art is human-accepted and seam-clean, and regenerating to refresh the prompt would discard approved art for a reason unrelated to motion.
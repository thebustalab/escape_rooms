I've read the pipeline, report, ledger (spindle sections), and `scene_spec.py`, and inspected the committed still at native resolution.

**What the evidence says about this room:**

- The sceneSpec already carries a bounded authored mover: `name_crag` has a complete `motion` block (`moves`/`vigour`/`phrase`), so `_has_authored_mover` sees it — this room is post-migration, not invisible to the loop.
- **The still actually depicts it.** The crop of the needle region shows a clear, well-formed spindrift banner tearing off the crest and feathering downwind — bounded (~15% of frame width, localized left-of-centre), not frame-filling. This is the `hood` failure mode's opposite: it is exactly the kind of mover that woke the three dead rooms.
- Historically spindle was *never* a dead room in the `hood`/`rams_head`/`ochre_hall` sense — the ledger records real spindrift motion at fixed end guide on seed 90210 (0.70) and Lucas's own winner label at 0.65. The "dead, dead, good" reading in `compare_seedtest.mp4` was 1536 seed-lottery behaviour, which per the 2026-09-11 decision does not govern at 3072.
- The motion prompt is the 38-word house form, resolves from the spec, and names that mover and nothing else.
- Gate report is `{}` and nothing has been tried: **no clip exists at this spec.**

Regenerating the still would discard a panorama whose seam Lucas accepted by hand on 2026-09-03 and whose mover is already correctly depicted — the expensive lever, spent on a room that doesn't need it. There is nothing to soften and nothing wrongly moving, because there is no clip to judge yet.

DECISION: RENDER
WHY: The spec carries a bounded mover that the committed 3072 still visibly depicts, the motion prompt names that mover in the house form, and no clip exists yet — so the one render the pipeline allows is the next step.
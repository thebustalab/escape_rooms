DIAGNOSIS: I extracted all 61 frames and read frames 0001, 0031 and 0061 whole, plus native-resolution crops of the `slab_ledger` box (x 1290–1690, y 727–850) at frames 0001/0031, the fire-basket at 0001/0031/0019, and the paving slab at 0031/0048. The ledger crop is pixel-identical across the loop — the open dispatch ledger, the filed copies and the weighting river stone do not lift, curl or shift, and its measured p95 of 0.56 sits exactly in the 0.31–0.86 band this pipeline records for a pin that is *working* on the bake (it is also below `WEAK_P95` = 8.0, so it should not have tripped `still_violations` at all). The only other frame-wide variation is a sub-pixel texture crawl on the rock: phase correlation on 100 px tiles between frames 0031 and 0048 returns shifts of 0–1 px (one tile at 2 px) and zero global displacement, which is what produces the large per-pixel diffs (p99 ≈ 91) with no visible change, and it matches the judge's own landmark_drift_px = 0 and rigid_frac 0.85. The three live subjects behave as specified — the cloud river boils over the lip and shreds on the western side without ever crossing the frame, the western river holds its channel with light sliding along it, the grass stirs; nothing rises, no avalanche, no flame ignites, no people or text.

One thing worth recording that is *not* a motion defect: the iron fire-basket in the committed `scene.png` already contains amber embers at its base, so it is present in every frame of every clip baked from this still. Over the loop it does not flicker, grow or gutter (warm-pixel count 125–220 out of 36 400, mean R steady at ~120, spikes only where the loop crossfade rides through) — the motion spec's fire pin is holding. That is a stills-phase question about the base art, not something a rerender or rebake of this clip can change.

DECISION: PASS
WHY: The pinned `slab_ledger` is visually and metrically frozen at native resolution across the whole loop with landmark drift at 0 px, so the report-only `still_violations` hit is the known false positive — sub-pixel texture crawl on a static object — and every other gate, subject and pinned constraint reads correct in frames 0001/0031/0061; the pre-existing embers in the fire-basket are baked into the committed source still and belong to the stills phase, not to this clip.
I've read the pipeline, the report, the ledger's lever register, and `scene_spec.py`, and inspected the current `scene.png` (regenerated today, 2026-09-14 22:46 — newer than the stale Sep-7 `cine_base.mp4`).

**What the evidence says about this room:**

- The spec carries exactly one bounded mover — `cascade`, with `motion.{moves, vigour, phrase}` populated, so `_has_authored_mover` can see it (not stranded in a legacy `animate` block).
- The regenerated still **depicts** it: a single ribbon of meltwater down the headwall left of centre, breaking white on a step and fanning into spray. Bounded, occupying a fraction of the frame — not a hood-style frame-filling drift subject.
- Depicted vigour is already vigorous and matches the `vigour` string; the motion prompt derives from `motion.phrase`, names that mover and nothing else, ~40 words, in the house form.
- Precedent in the ledger: the ladder waterfall was a clean accept before (clip 61, strongest tile 18.3 — "a waterfall legitimately moves that fast", which is why strongest-tile was rejected as a too-fast gate).

No clip exists against this still, the gate report is empty, and nothing has been tried this cycle. The spec and the prompt are both right, so there is nothing to author, adjust, soften or pin — the only unexercised step is the single render at the default settings.

DECISION: RENDER
WHY: The current still already depicts the authored bounded mover (one ribbon of meltwater with its vigour visible in the art) and the motion prompt derives cleanly from `motion.phrase`, so the spec is correct and no clip yet exists for this freshly regenerated panorama.
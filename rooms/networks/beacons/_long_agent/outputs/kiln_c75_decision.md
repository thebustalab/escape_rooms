Evidence read. Here's where `kiln` actually stands:

**Spec state.** The `2026-09-14 near_movers` pass replaced the distant `dome_banner` mover with a near bounded one: `juniper_smudge`, at `to the left`, carrying a proper `motion.{moves,vigour,phrase}` block. `dome_banner` now keeps only its legacy `animate` block, so `movers()` sees exactly one mover — the smoke.

**The still is current and it depicts the mover.** `scene.png` was regenerated today at 01:40 (with `scene_undo1.png` as its pre-edit backup). Viewed at native crop: an iron dish on a low tripod heaped with smouldering juniper, throwing a thick pale rope of smoke that climbs and rolls against the red dome — bounded, near the viewer, roughly a tenth of the frame width, with the depicted vigour matching the authored "smouldering thickly, its smoke climbing in a slow heavy rope". The fire-basket on the right is cold and unlit as the negatives require.

**The motion prompt in the brief is stale, and does not matter.** It names the banner cloud and heat shimmer — the pre-`near_movers` intent, and the `art_prompt_motion/beacons/kiln.txt` fallback. Since the spec declares a mover, `render_motion_prompt` supersedes it and emits:

> locked-off static camera, zero camera movement. The rope of juniper smoke climbing and rolling from the smouldering dish on its tripod. Only that moves; everything else stays perfectly still. Seamless natural loop.

One mover, one phrase, 33 words, house form. That is right.

**No clip exists for this spec.** `cine_base.mp4` is dated 2026-09-07, predating both the spec edit and today's still, and its `judge.json` was scored on `banner_cloud` — a subject the current authoring no longer drives. It cannot be read as this spec's result, and nothing about it justifies re-authoring art that now depicts a textbook bounded mover.

DECISION: RENDER
WHY: The spec carries one bounded, near-field mover with authored vigour, today's still visibly depicts it, the spec-derived motion prompt names it and nothing else, and no clip exists for this authoring.
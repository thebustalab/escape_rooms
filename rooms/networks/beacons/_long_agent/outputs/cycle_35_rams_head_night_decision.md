I have what I need. Frames examined: full-frame `0001`, native-res lamp-box crops at `0001/0020/0039/0058`, and a 4× zoom of the lamp cluster comparing `0001` and `0031`.

DIAGNOSIS: The panorama is healthy — the tarn, the horn, the spyglass and the basket all hold still, and at 4× zoom (frames 0001 vs 0031) the village lights are real but are literally 1–3-pixel warm specks: only ~90 warm pixels sit inside a 338×102 box, so they are 0.26% of their own bounding box and p95-over-the-box can never register them however hard they flicker. They are also genuinely flat in the shipped file — the mean of the 105 brightest lamp pixels moves 134.8→136.9 over the loop, a monotone drift with no wobble. But the chain tells the real story: the unpatched render `cine_night_src.mp4` carries the *most* lamp motion (box p95 4.16, hot-pixel peak-to-peak 7.0), the `village_lamps` repair tile that the last render auto-composited **reduced** it (p95 3.08, p-p 5.6 — the isolated 480×128 tile rendered the lamps flatter than the full frame did, exactly the "patch animating bare rock" case), and the loop crossfade took it down again to the shipped 2.79 / p-p 2.1. The mask is not the culprit — 0 of 105 hot lamp pixels are frozen and the box is 97.6% video.

DECISION: REBAKE
DROP_PATCHES: village_lamps
WHY: The auto-generated `village_lamps` repair tile measurably *destroyed* lamp motion that the base render already had (p-p 7.0 → 5.6 → 2.1 shipped), so rebuilding from `cine_night_src.mp4` with that patch disabled restores the liveliest lamps the pipeline has produced at zero GPU cost — and no rerender can do better, because a 90-pixel scatter of point lights inside a 34,814-pixel box is geometrically incapable of moving p95 past the 4.5 dead threshold even at perfect flicker, which is a spec/measurement mismatch for Lucas to settle rather than something new adjectives or another tile can fix.
I've read the pipeline, report, ledger, and the rams_head-specific history. Decision follows.

**What the evidence says about this exact clip:**

- **Camera: still.** No camera rejection.
- **Glitch: breadth 0.354 raw / 0.312 normalised.** The gate rejects only when *both* readings exceed 0.70. Both are less than half that. No glitch rejection — and `whistlegate`, an accepted clip, sat at exactly 0.354 raw.
- **Return 0.00 with peak 6.4.** The gate itself flags this as ambiguous and never rejects on it. The mover here is wind cat's-paws travelling across water — a **cyclic flow** mover, the exact class the ledger records as returning 0.00 while looping perfectly (b_casino's smoke, logwood_verdigris' water, both 0.00 at both settings and both alive by eye).

**And the precedent is on this very room.** `cinemagraph_ledger.md:1368` — *"Third confirmation that a zero return is not death. rams_head and crown re-authored measured return 0.00-0.02, which the gate flags as ambiguous, and Lucas saw them 'moving a lot'. The gate would have called two successful renders failures."* Line 1487 names rams_head again as one of the three times peak amplitude produced a wrong call on a cyclic mover, closing with *"for cyclic movers, no metric we have measures the thing Lucas sees."*

Peak 6.4 sits between hood's confirmed-dead 5.1-5.2 and rams_head's own confirmed-alive 7.76-7.96 — and calling it dead on that number would be the fourth instance of the one failure mode the ledger has retracted three times. Re-authoring the art on a peak-amplitude reading is precisely what the record forbids.

The spec carries one bounded mover the committed still depicts, cycle 8 corrected its phrase, cycle 9 rendered it, no gate rejected it, and no gate may approve it. That is the definition of a survivor.

DECISION: HOLD
WHY: The clip cleared every reject-only gate — camera still, glitch breadth less than half the 0.70 threshold on both readings — and its 0.00 return is the documented cyclic-flow signature of a wind-on-water mover, a call the ledger has already retracted twice on rams_head itself, so this goes to Lucas's eye rather than back to the art.
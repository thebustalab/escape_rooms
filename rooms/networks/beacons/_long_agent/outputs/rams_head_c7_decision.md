I've read the pipeline, report, ledger, and `scene_spec.py`, plus the live spec and the re-authoring experiment record.

The decisive finding: `rams_head`'s live spec contains **no lantern**. The 2026-09-13 `animate`→`motion` migration pasted the lantern phrase onto `cirque_tarn`, whose own vigour is about cat's-paws on the water. So `_has_authored_mover` returns true and the room looks render-ready, but the art prompt draws a tarn while the motion prompt asks for a swinging signal lantern — exactly the verdigris/alum_madder drift this design exists to prevent. The lantern is also the mover that provably woke this room (3-of-3 re-authoring experiment, `notes/reauthor/beacons__rams_head.spec.json`); it just never made it into the live spec.

I'm authoring it into the element the runner will match by `at`, so the fragment merges into `cirque_tarn` — the tarn stays depicted, the lantern leads the description, and the stale phrase/vigour are overwritten in the same edit, leaving exactly one mover. Vigour is written restrained deliberately (Lucas on the re-authored set: *"moving a lot... we're going to potentially need to dial things back"*), and the lantern is unlit to respect the spec's own "no glowing or luminous objects" and cold-beacon negatives — so the phrase drops the old "its light swings with it" claim.

DECISION: AUTHOR_MOVER
SPEC: {"at": "to the left of centre", "desc": "a signal lantern hanging free from a short iron gibbet arm at the platform's edge close to the viewer — a compact brass-and-glass lantern no bigger than two fists, unlit, its glass dull and empty, hanging on a single hook from a low rust-orange iron standard set into the drystone edging, a small hard-edged object read clearly against the dark wet wall behind it; directly below and beyond it the floor of the cirque, a small black tarn ringed with pale rubble and rimmed with a lace of unmelted ice, its surface dark and open", "motion": {"moves": true, "vigour": "the lantern caught hanging barely off vertical in the draught coming off the cirque wall, a small slow to-and-fro of no more than a hand's breadth", "phrase": "The unlit brass signal lantern hanging from its short iron gibbet arm sways gently to and fro on its hook, a small slow swing barely off vertical"}}
WHY: The still depicts no mover at all — the migration left a lantern phrase on the tarn element — so this authors the proven bounded lantern into the art and rewrites its phrase and vigour to match what will now be depicted.
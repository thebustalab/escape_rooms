DIAGNOSIS: I tracked the star field against frame 0001 by matching bright-pixel masks in the sky patch (x 1240–1860, y 60–300) and it translates coherently — (dx,dy) = (+10,+3) by f0011, (+16,+5) at f0016, (+24,+8) at f0026, (+28,+9) at f0031, then snaps to (−13,−3) at f0036 and eases back to (−1,0) at f0061 — while the same test on the foreground rock (x 300–900, y 760–980) sits at (0,0) with corr ≥0.984 for every frame, so this is the sky moving under a static ground, not camera drift, which is why `landmark_drift_px` reads 0 and only the report-only `background_swing` sees it. Looking at the brightened sky crops of 0001 vs 0016 the field is visibly reshuffled (zero overlapping bright pixels at zero shift, 203/413 recovered at dx=+16,dy=+5), and the f0031→f0036 snap is a hard ~41 px discontinuity mid-loop. The temporal-std profile explains why no slider can help: rows y 0.05–0.30 carry tstd 4.2–5.4 while `village_lamps` (y≈0.50–0.55) sits at 1.3–1.6 and `river_glimmer` (y≈0.65–0.70) at 1.7 — the sky is the strongest mover in the frame, so raising MASK_PCT would delete the two subjects and keep the drift. Everything above y=0.46 in the gridded still is sky, snowfields, ridgelines and dome rock — all of it declared `rigid` or `pinned`, with the highest skyline at y≈0.47 and the nearest live subject box starting at y=0.48.

DECISION: REBAKE
MASK_PCT: auto
DROP_PATCHES: none
WHY: I have written one `still: true` subject `sky_and_ridgelines` box [0.0,0.0,1.0,0.46] into the night spec (validated; `render_prompt` and `render_negative` are byte-identical, so the fixed-seed render is untouched) and `_mask_inputs` → `_still_boxes` will force that band black in the playback mask, freezing the drifting sky against the existing source for zero GPU.
DIAGNOSIS: The whole star field is boiling. In the shipped `cine_night.mp4` a grain-scale diff of the sky between frames 0001 and 0021 (/tmp/cine_look/x_skydiff.png) is a dense field of black/white dipoles across every degree of open sky, and a 4× zoom on one patch across frames 0001/0011/0021/0031 (/tmp/cine_look/x_starzoom.png) shows the star pattern completely reshuffling — different stars in different places each frame, with short comet-dashes on frame 0001. Per-row temporal std confirms it is confined to the sky: mean 2.0–2.6 from y=0.03 to y=0.31, falling to 0.7–0.9 below y=0.38 where the ridges and cloud sea sit, while the two wanted subjects (lamps y≈0.4–0.5, braids y≈0.5–0.6) sit entirely under that floor and read alive only on their sparse lit pixels. That is a flat breach of the spec's own pin, "The stars are fixed — the sky does not rotate, drift or streak"; the judge cannot see it because the sky is inside the rigid region and star swarm is spatially incoherent, so it barely moves the whole-region luma (background_swing 0.37) and never trips a gate. No threshold or region-cut rung can remove it — both mask axes select *for* motion, and the stars are exactly the fine speckle a region cut would delete alongside the point-source village lamps.

I applied the fix in the only shape the pipeline offers: `write_spec` with one added `still: true` subject `star_field [0.0, 0.0, 1.0, 0.38]` (validated; `render_prompt` output is byte-identical to before, so this is purely a bake-time pin and carries no render-side risk), then `apply_rebake` from `cine_night_src.mp4` with both repair tiles re-applied. Mask coverage went 69.8% → 38.1% video; sky temporal std is now 0.01, the pin boundary is invisible in the crop across it (/tmp/cine_after/x_seam.png), both subjects are still alive (lamps p95_lit 11.78, river 39.03), and every metric improved — frame0_corr 0.914 → 0.96, loop_jump 0.66 → 0.32, background_swing 0.37 → 0.26. The re-run below is idempotent and re-establishes the harness's own bookkeeping.

DECISION: REBAKE
DROP_PATCHES: none
WHY: The sky was swarming in violation of the spec's star pin, so I added a `still: true` `star_field` box over the top 38% of the frame — the only mechanism that can remove an unwanted mover — and rebaked from the unpatched source, freezing the stars while both wanted subjects keep their motion.
Evidence read. Here's what the gate report says once put against the ledger:

- **Camera: still** — passes.
- **Glitch: breadth 0.021 on both readings**, against a 0.70/both-readings threshold. Nowhere near. Passes.
- **Return 0.03** — the ambiguous flag, and the ledger settles which way it reads here. The mover is running water, the canonical *cyclic flow* case: a flowing mover never returns to its starting configuration pixel-wise. b_casino's smoke and logwood_verdigris's water both score 0.00 while looping perfectly; crown's streaming pennant returns 0.03-0.05 and Lucas saw it "moving a lot". Three good clips have already been called dead on this measure, and the gate's own note says so.
- **Peak 12.28** is the tell that separates cyclic flow from death when return is ~0. Dead hood sits at 5.1-5.2 across every end-guide pin; the live cyclic movers measure 8.06-9.35. This is above all of them — and the ledger already records the ladder waterfall at strongest-tile 18.3 as a clean accept, because a waterfall legitimately moves that fast.

The spec is correct as authored: one bounded mover (a single ribbon of meltwater beside the ladder head, not frame-filling), its vigour written into the `desc`, and the motion prompt derives verbatim from `cascade.motion.phrase` via `render_motion_prompt`. Nothing to re-author, and no metric may approve — so this goes to the eye, not to the GPU.

DECISION: HOLD
WHY: The clip cleared the camera and glitch gates outright, and its near-zero return is the documented cyclic-flow signature of a water mover rather than death — peak 12.28 sits well above the dead band (5.1-5.2) and above the known-live cyclic movers (8.06-9.35), so the only remaining step is Lucas's eye.
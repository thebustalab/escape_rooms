DIAGNOSIS: I extracted all 61 frames and read frame 0001 whole, then cropped both spec boxes at native resolution and the whole valley (x 950–2050, y 560–790) at 170%. The `village_lamps` box (px 921–1536 × 409–512) is indeed pure ridgeline and cloud with no lamps in it, and the `river_glimmer` box catches mostly empty cloud — but the subjects themselves plainly animate: over the sampled stack (0001/0008/0016/0023/0031/0038/0046/0053/0061) the warm lamp pixels breathe 80.6 → 85.7 luma and the bright braided-channel pixels glimmer 92.6 → 107.5, and a temporal-variance heatmap of the valley lights up exactly the lamp clusters and the river braids and nothing else — motion sits precisely on the two named subjects. The "dead" numbers are box dilution: lit pixels are 0.17% and 0.52% of those boxes, far under `SPARSE_FRAC` 5%, which is the exact case the sparse-lit rescue at `motion_spec.py:213-226` was written for (its comment names hood/night's `village_lamps` by name). Re-measuring the shipped file with the current `motion_spec` gives `p95_lit` 10.98 and 39.36 (34.27 / 34.25 under corrected boxes) — verdict `alive` for both — and re-running `cine_judge.py --chapter networks --scenario beacons --rooms hood` on the same unchanged mp4 now returns **HOLD, 0 rejected**.

DECISION: PASS
WHY: The rejection is stale — it was written before the sparse-lit-pixel path landed, and re-judging the identical shipped `cine_night.mp4` with the current code returns HOLD with both subjects alive (`p95_lit` 10.98 / 39.36), with the variance heatmap confirming the motion is on the lamps and river braids and nowhere else; the boxes are genuinely mis-aimed and worth correcting to `village_lamps [0.32,0.56,0.63,0.72]` / `river_glimmer [0.31,0.55,0.69,0.77]` as spec hygiene, but that is not worth a GPU rerender since the clip passes either way.
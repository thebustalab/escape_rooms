I've read the pipeline, the report, the ledger's `sisters` history, and `render_prompt`, then checked the live spec and art against them.

**What I found:**

- The spec carries a genuine **bounded authored mover**: `marker_line`, with a full `motion.{moves, vigour, phrase}` block — a cord of prayer-flag-style marker flags, the nearest an arm's length from the viewer. Hanging cloth is the ledger's strongest confirmed mover category (p95 up to 76.8), and it is bounded, not frame-filling.
- **The still actually depicts it.** I looked at `rooms/networks/beacons/sisters/scene.png` (3072x1024, regenerated 2026-09-14 22:00): the flag line on its two iron stakes is there, left of centre, large and near. This is the drift failure that killed verdigris and alum_madder, and it is not present here.
- `authoring.scenePrompt` is **byte-identical to `render_prompt(current spec)`** and carries the EQUIRECTANGULAR clause — no stale or hand-written prompt.
- The "current motion prompt" in my brief is the **stale `.txt` fallback about the cloud bank** — it is not what will be rendered. `exp_art_prompt.motion_prompt()` resolves from the spec and returns the flags phrase: *"The row of marker flags stirring gently along their cord, the nearest ones rippling slowly rather than snapping."* That's the correct, already-soft register.
- No clip exists for this art: the only `cine_base.mp4` is dated 2026-09-07, a week older than the still. Gate report is empty.

That frame-filling cloud river is exactly `hood`'s known failure mode at 3072, and pointing the prompt at it is what the ledger records as the historic sisters mistake (the grep that matched the wrong "river"). The spec already moved off it onto the flags. Nothing to author, nothing to soften — this room is ready for its one render.

DECISION: RENDER
WHY: The spec declares a bounded authored mover the committed still genuinely depicts (the near flag line), the prompt renders clean from that spec, and no clip exists for this art yet.
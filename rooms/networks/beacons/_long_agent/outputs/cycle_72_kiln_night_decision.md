I have what I need. The frames, the temporal-variance map, and the percentile ladder all point the same way.

**DIAGNOSIS:** The clip is genuinely wrong, and the movers are not the problem — frames 0001/0018/0035 look identical at a glance and both subjects animate correctly (the native-res crops in `c_lamps.png` and `c_river.png` show village windows breathing and the braided channels sparkling, exactly as authored). The defect is in the **sky**: the temporal-variance map over all 61 frames (`m_std.png`/`m_bin.png`) is filled edge-to-edge above the skyline with coherent diagonal **star streaks**, and cross-correlating a sky patch against frame 0001 confirms real translation — the field slides to dx=-11 px by frame 0011, decoheres past the ±12 px search window by frame 0016 (corr 0.148), swings back through dx=+12 at frames 0026–0031 and recloses at 0061 (corr 0.926). Zero-shift correlation collapses from 1.00 to 0.08 in eleven frames. That is precisely what the spec's own pinned line forbids ("the sky does not rotate, drift or streak"), and it explains the report-only numbers exactly: the declared `rigid` list names only ground furniture, so the sky sits outside the rigid mask — `rigid_frac` reads 0.95 and `landmark_drift_px` reads 0 because the *ground* really is nailed down, while the sky's whole-field motion leaks out as `background_swing` 0.62 / `rel_pct` 3.4 with no gate entitled to convict on it.

I checked the cheap fix first and it cannot work. Running the bake's own percentile ladder against `cine_night_src.mp4` shows the sky out-competing the subjects at **every single rung** — at p90 the sky retains 44% coverage while the `village_lamps` box holds only 14% and `river_glimmer` 5%; at p97 the sky still holds 12% against the river's 0.6%. This is the documented egypt/library failure written verbatim in `cine_scenario.py:86-94`: both mask axes select *for* motion, so neither can remove an unwanted mover. `auto` picked threshold 1.996 (≈p50, 88% coverage) because closing plus hole-filling solidifies the speckled starfield into one clean slab, which is why the drift ships. No MASK_PCT or MASK_REGION setting freezes the sky without starving the two subjects the clip exists to show, and there are no patches to drop. The remedy the codebase provides is the one it names — a region you want held still has to be *named*, via `still: True` boxes that are zeroed after the threshold, region cut and feather.

I validated the replacement spec before emitting it: `motion_spec.validate` returns no errors, the two still phrases de-duplicate into a single pin, the still subjects are correctly kept out of the positive mover list, and `cine_scenario` picks up both boxes. The box floors are measured, not guessed — I traced the horizon with guide lines across the full panorama (`h_L.png`, `h_M.png`, `sky_horizon.png`, `h_R.png`) and no sky falls below y=0.52 left of x=0.58 or below y=0.47 right of it, while both mover boxes (y≥0.48 and y≥0.61) stay entirely clear.

DECISION: RERENDER

```json
{"rigid": "The night sky and the whole field of stars, the distant snow range and its skyline, the stone platform and parapet, the timber signal mast and plinth, the fuel-store, the cold iron fire-basket, the spyglass and tripod, the paths, the rock, the ridgelines and the moonlit snowfields", "pinned": ["The river is a fixed level: it does not rise, swell, flood, spread or advance.", "The stars are fixed — the sky does not rotate, drift or streak, and no star trails appear.", "The snowfields and ridgelines are fixed: no avalanche, no sliding snow, no shifting rock.", "Every iron fire-basket on every tower is COLD, empty and unlit — no flame, no ember, no glow, and no fire appears anywhere on the range."], "negatives": [", rising water, flooding, water level changing, surging water, waves, splashing, avalanche, sliding snow, star trails, rotating sky, moving stars, camera motion, panning, zooming, parallax, people, figures, new lights appearing, fire spreading, sunrise, daylight, dawn"], "has_document": true, "subjects": [{"name": "village_lamps", "box": [0.58, 0.48, 0.79, 0.57], "phrase": "the scattered points of village lamplight far below on the benches breathing and flickering very gently, a few of them wavering as if seen through cold air"}, {"name": "river_glimmer", "box": [0.63, 0.61, 0.79, 0.79], "phrase": "the pale braided channels on the valley floor glimmering faintly and sliding very slowly in place, the wet gravel bars catching and losing the light"}, {"name": "sky_left", "box": [0.0, 0.0, 0.58, 0.52], "still": true, "phrase": "The starfield above the ridgelines is a fixed photograph: every star holds its exact position and the sky does not turn."}, {"name": "sky_right", "box": [0.58, 0.0, 1.0, 0.47], "still": true, "phrase": "The starfield above the ridgelines is a fixed photograph: every star holds its exact position and the sky does not turn."}], "room": "kiln", "state": "night"}
```

WHY: The single change is naming the sky as a region that must be held still — two `still: True` boxes that the bake zeroes after the threshold and feather (plus the sky added to `rigid`, which had listed only ground furniture) — because the drifting starfield out-measures both subjects at every mask percentile, so nothing short of an enforced still box can freeze it without killing the lamps and the river.
DIAGNOSIS: Frame 0001 at native resolution shows the horizon at y≈460 px, so the `glacier_tarn` box [0.3,0.3,0.4,0.4] (px 921–1229 × 307–410) sits entirely in the star field — there is no tarn, no moraine and no glacier in it, which is why it measures p95 1.89 and reads "dead"; the `village_lamps` and `river_glimmer` boxes are likewise mostly rigid ridgeline and upper valley. The real tarn is the pale lake at px ~843–978 × 625–668 (2× crop of frames 0001/0031), the lamp clusters run px ~1100–1850 × 615–705, and the braided channels px ~1050–1800 × 630–740. Re-measuring the *shipped* clip on those corrected boxes gives river 4.48 (p95_lit 11.01) alive, lamps 4.65 (11.70) alive, tarn 3.32 (6.64) weak — no dead subject and an empty repair_list; the temporal-std map over all 61 frames confirms the tarn water itself is quiet but not zeroed, and the src/raw renders measure the same, so the mask is not pinning it either. I have written the three corrected boxes into `scenario.json` via `cine_iterate.write_spec` (validated, backed up as `scenario.json.bak_*_cine_iterate_boxfix_c02`); the only thing left is to rebuild the bake without the repair tile attempt 1 fired into the star field.

DECISION: REBAKE
DROP_PATCHES: glacier_tarn
WHY: The reject was an artifact of three mis-aimed subject boxes (now corrected in the spec) and of a repair patch pasted into empty sky — rebaking from `cine_night_src.mp4` with that patch dropped re-judges as hold (alive/alive/weak) with no GPU work.
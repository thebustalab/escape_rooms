DIAGNOSIS: I pulled native crops of x 706–1013, y 512–799 at frames 1, 13, 25 and 43 and the brass T-stand standing behind the scale beam on the sorting bench is not the same object frame to frame — cross-armed with a knob at f1, a wider bowl/hook head at f13, cross-armed again at f25, a flared knobbed top at f43 — while the scale beam, its pans and everything else on the bench sit dead still, so this is latent boiling on a rigid prop, not motion. A per-tile temporal-std sweep of the whole panorama makes it the single hottest 32×32 tile in the frame (mean 15.05 at x=736,y=576, above hearth_fire's 13.67 and hearth_smoke's ~12), which is exactly the low-level rigid-region churn that lifts `background_swing` and `still_violations` into report-only territory without tripping any threshold, since the box is small and none of the named subjects sits on it. The right-hand hot patches (frac 0.77–0.87 × 0.62–0.94) are the sunlit flagstones outside the NE door and compare identically at f1 vs f25 — that is authored daylight shimmer, not a defect. The spec carrying the fix is already on disk: I validated and wrote a `bench_scales` still subject at [0.23, 0.50, 0.33, 0.78] (clears `gate_daylight`'s 0.22 edge, `states.night` preserved), because the REBAKE handler in `long_agent.py` never calls `write_spec` and only `_still_boxes`, re-read from `scenario.json` at bake time, can carry it.

DECISION: REBAKE
DROP_PATCHES: none
WHY: The mask sidecar is enabled at auto and already pins `desk_ledger`, so re-baking from `cine_base_src.mp4` will pick up the newly authored `bench_scales` still box the same way and freeze the boiling brass stand for no GPU, leaving the fire, smoke and daylight — all currently passing — untouched.
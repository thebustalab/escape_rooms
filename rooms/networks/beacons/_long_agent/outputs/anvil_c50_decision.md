**Evidence read; here is the state of `anvil`.**

- The spec now carries exactly one bounded mover under the live `motion` schema: `fuel_tarp` — a tarpaulin corner torn free and flogging, in the near field, on the left. Hanging cloth/canvas is the strongest confirmed mover category in the ledger (p95 up to 76.8), and it is bounded rather than frame-filling.
- The vigour is authored into it (`"one corner torn free and flogging hard in the open wind"`), so `element_desc` fed it into the art prompt.
- `scene.png` (23:08:22) and `scenario.json` (23:08:27) are five seconds apart — the committed still came from *this* spec. The current still therefore depicts the tarp.
- `render_motion_prompt` resolves from the spec and returns the correct ~30-word motion-only form: *"locked-off static camera, zero camera movement. The loose corner of the canvas tarpaulin over the fuel stack flogging and cracking in the wind. Only that moves; everything else stays perfectly still. Seamless natural loop."* The prompt quoted in my brief (heat shimmer + depot smoke) is the stale `.txt`/legacy-`animate` text, superseded by the spec — and correctly so: the shimmer is the whole pavement, i.e. frame-filling, exactly the mover shape that reads as nothing at 3072.
- `cine_base.mp4` is from 2026-09-07, a week older than the still, and from the era when anvil measured borderline with the motion in the furniture. There is no clip against the current art, and the gate report is empty.

Nothing to author, nothing to soften, nothing to pin — the room is authored and un-rendered.

DECISION: RENDER
WHY: The current still was committed from a spec carrying one bounded mover with authored vigour (the flogging tarpaulin corner), the spec-derived motion prompt names only that mover, and no clip exists against this art — the only stale clip predates the still by a week.
DIAGNOSIS: The `beacon_fires` box `[0.12,0.28,0.14,0.32]` lands on empty star-field sky — at native resolution that is pixels x 368–430, y 286–327, whose mean luma over the loop is 1.6 and whose p95 is 0.3 (I cropped it from the source still and from frames 0001/0015/0030/0045/0060 and it contains nothing but fixed stars, which the spec correctly pins). The actual ridge-shoulder beacon sits ~110 px right and ~150 px below that, at roughly x 470–520, y 440–480 (fractional ≈ 0.15,0.43–0.17,0.47); cropped tight and tiled across frames 0001/0010/0020/0030/0040/0050 it is guttering, flaring and changing shape every frame, measuring p95 30.95 inside the flame and 12.52 including its glow — vigorously alive, not dead. The already-enabled `beacon_fires` patch in `cine_order_sent.patches.json` therefore had nothing to act on inside the box, which is why attempt 1's rerender changed the number not at all. The background_swing warn is the same scene behaving correctly: frames 0003 (min luma 13.67) and 0033 (max 21.65) show the summit brazier going from a low ember bed to a tall streaming flame without ever going out, beating its light across the slabs and shutter rack — a human-tier warn in a flame-lit night room, not a convicting fault.

DECISION: PASS
WHY: The only convicting finding is a measurement artefact — `beacon_fires` is measured over a patch of pinned night sky rather than the flame, which sits at about `[0.148,0.415,0.176,0.483]` and measures p95 31; the spec box should be moved there so the check stops misfiring, but the shipped clip itself is correct and needs no render or bake.
{
  "name": "escape-rooms-js-tests",
  "private": true,
  "description": "Node unit tests for the escape-rooms browser JS (pure logic). Run: npm test (or node --test).",
  "scripts": {
    "test": "node --test net_graph.test.js open_target.test.js codec.test.js cand_state.test.js door_graph.test.js variant_resolve.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "^1.61.1"
  }
}
